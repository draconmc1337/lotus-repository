# musl compatibility patch for groff
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-input.cpp.patch.patch

# Issue: input.cpp:34 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/input.cpp
+++ b/input.cpp
@@ -34,1 +34,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: input.cpp:329 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/input.cpp
+++ b/input.cpp
@@ -329,1 +329,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: input.cpp:225 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -225,1 +225,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: input.cpp:287 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -287,1 +287,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: input.cpp:397 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -397,1 +397,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: input.cpp:435 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -435,1 +435,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: input.cpp:696 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -696,1 +696,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: input.cpp:699 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -699,1 +699,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: input.cpp:785 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -785,1 +785,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: input.cpp:3492 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -3492,1 +3492,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: input.cpp:3602 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -3602,1 +3602,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: input.cpp:8811 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/input.cpp
+++ b/input.cpp
@@ -8811,1 +8811,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

