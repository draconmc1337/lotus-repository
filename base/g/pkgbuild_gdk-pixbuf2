# pkgbuild_gdk-pixbuf2
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "gdk-pixbuf2"
pkgver  = "2.42.12"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ("glib-2.0"; "gobject-2.0"; "gmodule-no-export-2.0"; "gio-2.0"; "shared-mime-info"; "libpng"; "libjpeg"; "libjpeg-turbo")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://download.gnome.org/sources/gdk-pixbuf/2.42/gdk-pixbuf-2.42.12.tar.xz"
md5sums = "b9505b3445b9a7e48ced34760c3bcb73e966df3ac94c95a148cb669ab748e3c7"

prepare() {
	cd "$srcdir/gdk-pixbuf-2.42.12  # extracted dir: gdk-pixbuf-2.42.12"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/gdk-pixbuf-2.42.12  # extracted dir: gdk-pixbuf-2.42.12"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/gdk-pixbuf-2.42.12  # extracted dir: gdk-pixbuf-2.42.12"
	ninja -C build test
}

package() {
	cd "$srcdir/gdk-pixbuf-2.42.12  # extracted dir: gdk-pixbuf-2.42.12"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
