# musl compatibility patch for grep
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-wctype.in.h.patch.patch

# Issue: wctype.in.h:599 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: wctype.in.h:619 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: wctype.in.h:637 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: wctype.in.h:665 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: wctype.in.h:683 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: wctype.in.h:701 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: wctype.in.h:720 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

