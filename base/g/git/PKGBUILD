# PKGBUILD — git
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "git"
pkgver  = "2.47.1"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "openssl"
    "zlib"
    "curl"
    "glib2"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://www.kernel.org/pub/software/scm/git/git-2.47.1.tar.xz"
    "patches/musl-t-reftable-stack.patch"
    "patches/musl-t-reftable-readwrite.patch"
    "patches/musl-t-reftable-merged.patch"
    "patches/musl-t-reftable-block.patch"
    "patches/musl-test-reftable.patch"
    "patches/musl-writer.patch"
    "patches/musl-stack.patch"
    "patches/musl-record.patch"
    "patches/musl-reader.patch"
    "patches/musl-merged.patch"
    "patches/musl-iter.patch"
    "patches/musl-error.patch"
    "patches/musl-blocksource.patch"
    "patches/musl-block.patch"
    "patches/musl-reftable-backend.patch"
    "patches/musl-kwset.patch"
    "patches/musl-obstack.patch"
    "patches/musl-obstack.patch"
    "patches/musl-compiler.patch"
)

checksums = (
    "sha512:6abe551c464b307bc3f6f474257e0be3e1a9eba1406af6463216b796c55a35356009c2f7bd9b4fa2d1798da5f885a3843f6ad8750ab69595f748f9ea8ed76fea"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-t-reftable-stack.patch"
	patch -p1 < "$srcdir/patches/musl-t-reftable-readwrite.patch"
	patch -p1 < "$srcdir/patches/musl-t-reftable-merged.patch"
	patch -p1 < "$srcdir/patches/musl-t-reftable-block.patch"
	patch -p1 < "$srcdir/patches/musl-test-reftable.patch"
	patch -p1 < "$srcdir/patches/musl-writer.patch"
	patch -p1 < "$srcdir/patches/musl-stack.patch"
	patch -p1 < "$srcdir/patches/musl-record.patch"
	patch -p1 < "$srcdir/patches/musl-reader.patch"
	patch -p1 < "$srcdir/patches/musl-merged.patch"
	patch -p1 < "$srcdir/patches/musl-iter.patch"
	patch -p1 < "$srcdir/patches/musl-error.patch"
	patch -p1 < "$srcdir/patches/musl-blocksource.patch"
	patch -p1 < "$srcdir/patches/musl-block.patch"
	patch -p1 < "$srcdir/patches/musl-reftable-backend.patch"
	patch -p1 < "$srcdir/patches/musl-kwset.patch"
	patch -p1 < "$srcdir/patches/musl-obstack.patch"
	patch -p1 < "$srcdir/patches/musl-obstack.patch"
	patch -p1 < "$srcdir/patches/musl-compiler.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
