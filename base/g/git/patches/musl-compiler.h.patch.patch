# musl compatibility patch for git
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-compiler.h.patch.patch

# Issue: compiler.h:6 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: compiler.h:7 — gnu/libc-version.h
# Fix:   remove include, not needed on musl

# Issue: compiler.h:32 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

