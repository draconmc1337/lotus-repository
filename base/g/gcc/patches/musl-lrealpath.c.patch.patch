# musl compatibility patch for gcc
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-lrealpath.c.patch.patch

# Issue: lrealpath.c:56 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/lrealpath.c
+++ b/lrealpath.c
@@ -56,1 +56,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

# Issue: lrealpath.c:220 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/lrealpath.c
+++ b/lrealpath.c
@@ -220,1 +220,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

# Issue: lrealpath.c:224 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/lrealpath.c
+++ b/lrealpath.c
@@ -224,1 +224,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

