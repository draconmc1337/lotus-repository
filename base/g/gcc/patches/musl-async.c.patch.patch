# musl compatibility patch for gcc
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-async.c.patch.patch

# Issue: async.c:114 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/async.c
+++ b/async.c
@@ -114,1 +114,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: async.c:168 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/async.c
+++ b/async.c
@@ -168,1 +168,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: async.c:258 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/async.c
+++ b/async.c
@@ -258,1 +258,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: async.c:387 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/async.c
+++ b/async.c
@@ -387,1 +387,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: async.c:393 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/async.c
+++ b/async.c
@@ -393,1 +393,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: async.c:420 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/async.c
+++ b/async.c
@@ -420,1 +420,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

