# pkgbuild_gtk3
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "gtk3"
pkgver  = "3.24.48"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ("glib-2.0"; "gobject-2.0"; "gio-windows-2.0"; "gio-unix-2.0"; "gmodule-2.0"; "cairo"; "cairo-gobject"; "pango"; "fribidi"; "pangoft2"; "pangowin32"; "pangocairo"; "gdk-pixbuf-2.0"; "epoxy"; "atk"; "harfbuzz"; "xkbcommon"; "iso-codes"; "tracker-sparql-3.0"; "wayland-client"; "wayland-protocols"; "wayland-cursor"; "wayland-egl"; "xrandr"; "x11"; "xrender"; "xi"; "xext"; "xcursor"; "xdamage"; "xfixes"; "xcomposite"; "fontconfig"; "atk-bridge-2.0"; "xinerama"; ""; "cloudproviders"; "sysprof-capture-3"; "sysprof-capture-4"; "gtk+3")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://download.gnome.org/sources/gtk/3.24/gtk-3.24.48.tar.xz"
md5sums = "d10ce9ea9df44c1016d8d1721f39e55d3d607fcfb85334aec0d236cdc9a70556"

prepare() {
	cd "$srcdir/gtk-3.24.48  # extracted dir: gtk-3.24.48"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/gtk-3.24.48  # extracted dir: gtk-3.24.48"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/gtk-3.24.48  # extracted dir: gtk-3.24.48"
	ninja -C build test
}

package() {
	cd "$srcdir/gtk-3.24.48  # extracted dir: gtk-3.24.48"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
