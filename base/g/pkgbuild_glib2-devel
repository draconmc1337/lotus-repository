# pkgbuild_glib2-devel
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "glib2-devel"
pkgver  = "2.82.5"
pkgrel  = "1"
pkgdesc = "Defined if strerror_r returns char *"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = (""; "iconv"; "libpcre2-8"; "gvdb"; "libffi"; "zlib"; "intl"; "mount"; "libselinux"; "bash-completion")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://download.gnome.org/sources/glib/2.82/glib-2.82.5.tar.xz"
md5sums = "05c2031f9bdf6b5aba7a06ca84f0b4aced28b19bf1b50c6ab25cc675277cbc3f"

prepare() {
	cd "$srcdir/glib-2.82.5  # extracted dir: glib-2.82.5"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/glib-2.82.5  # extracted dir: glib-2.82.5"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/glib-2.82.5  # extracted dir: glib-2.82.5"
	ninja -C build test
}

package() {
	cd "$srcdir/glib-2.82.5  # extracted dir: glib-2.82.5"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
