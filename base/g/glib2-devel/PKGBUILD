# PKGBUILD — glib2-devel
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "glib2-devel"
pkgver  = "2.82.4"
pkgrel  = "1"
pkgdesc = "Defined if strerror_r returns char *"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "glib2"
    "glibc"
    "libelf"
    "python"
    "python-packaging"
)

makedepends = (
    "iconv"
    "libpcre2-8"
    "gvdb"
    "libffi"
    "zlib"
    "intl"
    "mount"
    "libselinux"
    "bash-completion"
    "glib2"
    "dbus"
    "dconf"
    "gettext"
    "gi-docgen"
    "git"
    "gobject-introspection"
    "libelf"
    "meson"
    "python"
    "python-docutils"
    "python-packaging"
    "shared-mime-info"
    "util-linux"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://download.gnome.org/sources/glib/2.82/glib-2.82.4.tar.xz"
    "patches/musl-error.patch"
    "patches/musl-gvariant-parser.patch"
    "patches/musl-gunicode.patch"
    "patches/musl-gtimezone.patch"
    "patches/musl-gthread.patch"
    "patches/musl-gtestutils.patch"
    "patches/musl-gstrfuncs.patch"
    "patches/musl-gspawn.patch"
    "patches/musl-gspawn-posix.patch"
    "patches/musl-gshell.patch"
    "patches/musl-gregex.patch"
    "patches/musl-goption.patch"
    "patches/musl-vasnprintf.patch"
    "patches/musl-printf-parse.patch"
    "patches/musl-printf-parse.patch"
    "patches/musl-gmarkup.patch"
    "patches/musl-gmappedfile.patch"
    "patches/musl-gmappedfile.patch"
    "patches/musl-glib.patch"
    "patches/musl-gkeyfile.patch"
    "patches/musl-gkeyfile.patch"
    "patches/musl-giounix.patch"
    "patches/musl-gfileutils.patch"
    "patches/musl-gerror.patch"
    "patches/musl-gerror.patch"
)

checksums = (
    "sha512:5df0b3c56fe6cf6c94019e29afb408174a5864cdb97cf58d5a12a0f83990b5c2004a49c8e3b0e195f5808874b5911a4a7b3d6aea8718aada1b40561e623cd08d"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/glib-2.82.4"
	patch -p1 < "$srcdir/patches/musl-error.patch"
	patch -p1 < "$srcdir/patches/musl-gvariant-parser.patch"
	patch -p1 < "$srcdir/patches/musl-gunicode.patch"
	patch -p1 < "$srcdir/patches/musl-gtimezone.patch"
	patch -p1 < "$srcdir/patches/musl-gthread.patch"
	patch -p1 < "$srcdir/patches/musl-gtestutils.patch"
	patch -p1 < "$srcdir/patches/musl-gstrfuncs.patch"
	patch -p1 < "$srcdir/patches/musl-gspawn.patch"
	patch -p1 < "$srcdir/patches/musl-gspawn-posix.patch"
	patch -p1 < "$srcdir/patches/musl-gshell.patch"
	patch -p1 < "$srcdir/patches/musl-gregex.patch"
	patch -p1 < "$srcdir/patches/musl-goption.patch"
	patch -p1 < "$srcdir/patches/musl-vasnprintf.patch"
	patch -p1 < "$srcdir/patches/musl-printf-parse.patch"
	patch -p1 < "$srcdir/patches/musl-printf-parse.patch"
	patch -p1 < "$srcdir/patches/musl-gmarkup.patch"
	patch -p1 < "$srcdir/patches/musl-gmappedfile.patch"
	patch -p1 < "$srcdir/patches/musl-gmappedfile.patch"
	patch -p1 < "$srcdir/patches/musl-glib.patch"
	patch -p1 < "$srcdir/patches/musl-gkeyfile.patch"
	patch -p1 < "$srcdir/patches/musl-gkeyfile.patch"
	patch -p1 < "$srcdir/patches/musl-giounix.patch"
	patch -p1 < "$srcdir/patches/musl-gfileutils.patch"
	patch -p1 < "$srcdir/patches/musl-gerror.patch"
	patch -p1 < "$srcdir/patches/musl-gerror.patch"
}

build() {
	cd "$srcdir/glib-2.82.4"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/glib-2.82.4"
	ninja -C build test
}

package() {
	cd "$srcdir/glib-2.82.4"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
