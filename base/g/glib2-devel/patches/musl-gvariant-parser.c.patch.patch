# musl compatibility patch for glib2-devel
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-gvariant-parser.c.patch.patch

# Issue: gvariant-parser.c:28 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/gvariant-parser.c
+++ b/gvariant-parser.c
@@ -28,1 +28,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

