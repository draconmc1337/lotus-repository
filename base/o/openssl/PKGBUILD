# PKGBUILD — openssl
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "openssl"
pkgver  = "3.4.0"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "Apache-2.0"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "brotli"
    "glibc"
    "zlib"
    "zstd"
)

makedepends = (
    "perl"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/openssl/openssl/releases/download/openssl-3.4.0/openssl-3.4.0.tar.gz"
    "patches/musl-shlibloadtest.patch"
    "patches/musl-quicfaultstest.patch"
    "patches/musl-quicapitest.patch"
    "patches/musl-quic_newcid_test.patch"
    "patches/musl-quic_multistream_test.patch"
    "patches/musl-quic_fc_test.patch"
    "patches/musl-quictestlib.patch"
    "patches/musl-quic_wire.patch"
    "patches/musl-quic_txp.patch"
    "patches/musl-quic_tls.patch"
    "patches/musl-quic_rx_depack.patch"
    "patches/musl-quic_impl.patch"
    "patches/musl-quic_fc.patch"
    "patches/musl-quic_channel.patch"
    "patches/musl-qlog_event_helpers.patch"
    "patches/musl-rand_unix.patch"
    "patches/musl-uid.patch"
    "patches/musl-s390xcap.patch"
    "patches/musl-ppccap.patch"
    "patches/musl-getenv.patch"
    "patches/musl-bss_dgram.patch"
    "patches/musl-async_posix.patch"
    "patches/musl-armcap.patch"
)

checksums = (
    "sha512:0784096f00c7907e477919d5ddeadb14b61bcb569a938fa739c1c714949214a7daf63574149d718dae372ed0c91c300042f4e3ba5e8633607e8034a3bda75a26"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-shlibloadtest.patch"
	patch -p1 < "$srcdir/patches/musl-quicfaultstest.patch"
	patch -p1 < "$srcdir/patches/musl-quicapitest.patch"
	patch -p1 < "$srcdir/patches/musl-quic_newcid_test.patch"
	patch -p1 < "$srcdir/patches/musl-quic_multistream_test.patch"
	patch -p1 < "$srcdir/patches/musl-quic_fc_test.patch"
	patch -p1 < "$srcdir/patches/musl-quictestlib.patch"
	patch -p1 < "$srcdir/patches/musl-quic_wire.patch"
	patch -p1 < "$srcdir/patches/musl-quic_txp.patch"
	patch -p1 < "$srcdir/patches/musl-quic_tls.patch"
	patch -p1 < "$srcdir/patches/musl-quic_rx_depack.patch"
	patch -p1 < "$srcdir/patches/musl-quic_impl.patch"
	patch -p1 < "$srcdir/patches/musl-quic_fc.patch"
	patch -p1 < "$srcdir/patches/musl-quic_channel.patch"
	patch -p1 < "$srcdir/patches/musl-qlog_event_helpers.patch"
	patch -p1 < "$srcdir/patches/musl-rand_unix.patch"
	patch -p1 < "$srcdir/patches/musl-uid.patch"
	patch -p1 < "$srcdir/patches/musl-s390xcap.patch"
	patch -p1 < "$srcdir/patches/musl-ppccap.patch"
	patch -p1 < "$srcdir/patches/musl-getenv.patch"
	patch -p1 < "$srcdir/patches/musl-bss_dgram.patch"
	patch -p1 < "$srcdir/patches/musl-async_posix.patch"
	patch -p1 < "$srcdir/patches/musl-armcap.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	# TODO: add build commands
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	# TODO: run tests
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	# TODO: install files
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
