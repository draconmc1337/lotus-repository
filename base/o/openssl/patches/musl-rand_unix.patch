# musl compatibility patch for openssl
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-rand_unix.patch

# Issue: rand_unix.c:67 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: rand_unix.c:81 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

