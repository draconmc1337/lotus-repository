# musl compatibility patch for libnl
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-libc-compat.h.patch.patch

# Issue: libc-compat.h:53 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: libc-compat.h:173 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: libc-compat.h:265 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

