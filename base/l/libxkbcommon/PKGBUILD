# PKGBUILD — libxkbcommon
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "libxkbcommon"
pkgver  = "1.7.0"
pkgrel  = "1"
pkgdesc = "XKB API common to servers and clients"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "xkeyboard-config"
    "xkbcommon"
    "xcb"
    "xcb-xkb"
    "xkbcommon-x11"
    "libxml-2.0"
    "xkbregistry"
    "bash-completion"
    "wayland-client"
    "wayland-protocols"
    "wayland-scanner"
    "icu-uc"
    "wayland"
    "libxcb"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/xkbcommon/libxkbcommon/archive/refs/tags/xkbcommon-1.7.0.tar.gz"
)

checksums = (
    "sha512:454fbb2861405ca957d64035e924c1bbb7d43db7867903963fc053b7ecb64a8fba89a21cc8ac18ebeec9b61ae0789fb88c52521a850dc371857f28b08e80167b"
)

prepare() {
	cd "$srcdir/libxkbcommon-xkbcommon-1.7.0"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/libxkbcommon-xkbcommon-1.7.0"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/libxkbcommon-xkbcommon-1.7.0"
	ninja -C build test
}

package() {
	cd "$srcdir/libxkbcommon-xkbcommon-1.7.0"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
