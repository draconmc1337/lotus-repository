# PKGBUILD — linux-headers
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "linux-headers"
pkgver  = "6.12.6"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "GPL-2.0"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "binutils"
    "glibc"
    "libelf"
    "libgcc"
    "openssl"
    "pahole"
    "xxhash"
    "zlib"
    "zstd"
)

makedepends = (
    "bc"
    "binutils"
    "cpio"
    "gettext"
    "glibc"
    "graphviz"
    "imagemagick"
    "libelf"
    "libgcc"
    "openssl"
    "pahole"
    "perl"
    "python"
    "python-sphinx"
    "python-yaml"
    "rust"
    "rust-bindgen"
    "rust-src"
    "tar"
    "texlive-latexextra"
    "xxhash"
    "xz"
    "zlib"
    "zstd"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.12.6.tar.xz"
    "patches/musl-vsock_uring_test.patch"
    "patches/musl-unwind_vdso.patch"
    "patches/musl-ptrace_syscall.patch"
    "patches/musl-tm-unavailable.patch"
    "patches/musl-tm-trap.patch"
    "patches/musl-nolibc-test.patch"
    "patches/musl-udpgso_bench_tx.patch"
    "patches/musl-udpgso_bench_rx.patch"
    "patches/musl-udpgso.patch"
    "patches/musl-txtimestamp.patch"
    "patches/musl-txring_overwrite.patch"
    "patches/musl-toeplitz.patch"
    "patches/musl-tls.patch"
    "patches/musl-tcp_mmap.patch"
    "patches/musl-tcp_inq.patch"
    "patches/musl-tcp_fastopen_backup_key.patch"
    "patches/musl-stress_reuseport_listen.patch"
    "patches/musl-so_txtime.patch"
    "patches/musl-sk_connect_zero_addr.patch"
    "patches/musl-sk_bind_sendto_listen.patch"
    "patches/musl-rxtimestamp.patch"
    "patches/musl-reuseport_dualstack.patch"
    "patches/musl-reuseport_bpf_numa.patch"
    "patches/musl-reuseport_bpf_cpu.patch"
    "patches/musl-reuseport_bpf.patch"
    "patches/musl-reuseport_addr_any.patch"
    "patches/musl-reuseaddr_conflict.patch"
    "patches/musl-psock_snd.patch"
    "patches/musl-ncdevmem.patch"
)

checksums = (
    "sha512:35fcfc5044b1bf892b6432a330df26c5601f78f78f416400359f5e08c448255bcf424de611e67ff7ac72d014884d5f3c6c2bddb20d08dd47469df5c0424832f9"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/linux-6.12.6"
	patch -p1 < "$srcdir/patches/musl-vsock_uring_test.patch"
	patch -p1 < "$srcdir/patches/musl-unwind_vdso.patch"
	patch -p1 < "$srcdir/patches/musl-ptrace_syscall.patch"
	patch -p1 < "$srcdir/patches/musl-tm-unavailable.patch"
	patch -p1 < "$srcdir/patches/musl-tm-trap.patch"
	patch -p1 < "$srcdir/patches/musl-nolibc-test.patch"
	patch -p1 < "$srcdir/patches/musl-udpgso_bench_tx.patch"
	patch -p1 < "$srcdir/patches/musl-udpgso_bench_rx.patch"
	patch -p1 < "$srcdir/patches/musl-udpgso.patch"
	patch -p1 < "$srcdir/patches/musl-txtimestamp.patch"
	patch -p1 < "$srcdir/patches/musl-txring_overwrite.patch"
	patch -p1 < "$srcdir/patches/musl-toeplitz.patch"
	patch -p1 < "$srcdir/patches/musl-tls.patch"
	patch -p1 < "$srcdir/patches/musl-tcp_mmap.patch"
	patch -p1 < "$srcdir/patches/musl-tcp_inq.patch"
	patch -p1 < "$srcdir/patches/musl-tcp_fastopen_backup_key.patch"
	patch -p1 < "$srcdir/patches/musl-stress_reuseport_listen.patch"
	patch -p1 < "$srcdir/patches/musl-so_txtime.patch"
	patch -p1 < "$srcdir/patches/musl-sk_connect_zero_addr.patch"
	patch -p1 < "$srcdir/patches/musl-sk_bind_sendto_listen.patch"
	patch -p1 < "$srcdir/patches/musl-rxtimestamp.patch"
	patch -p1 < "$srcdir/patches/musl-reuseport_dualstack.patch"
	patch -p1 < "$srcdir/patches/musl-reuseport_bpf_numa.patch"
	patch -p1 < "$srcdir/patches/musl-reuseport_bpf_cpu.patch"
	patch -p1 < "$srcdir/patches/musl-reuseport_bpf.patch"
	patch -p1 < "$srcdir/patches/musl-reuseport_addr_any.patch"
	patch -p1 < "$srcdir/patches/musl-reuseaddr_conflict.patch"
	patch -p1 < "$srcdir/patches/musl-psock_snd.patch"
	patch -p1 < "$srcdir/patches/musl-ncdevmem.patch"
}

build() {
	cd "$srcdir/linux-6.12.6"
	make -j$(nproc) PREFIX=/usr
}

package() {
	cd "$srcdir/linux-6.12.6"
	make install PREFIX="$pkgdir/usr"
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
