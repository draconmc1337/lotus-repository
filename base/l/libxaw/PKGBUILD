# PKGBUILD — libxaw
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "libxaw"
pkgver  = "1.0.16"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "glibc"
    "libx11"
    "libxext"
    "libxmu"
    "libxpm"
    "libxt"
    "xorgproto"
)

makedepends = (
    "glib2"
    "xorg-util-macros"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://www.x.org/releases/individual/lib/libXaw-1.0.16.tar.xz"
)

checksums = (
    "sha512:1cabad1a9b06dec7de601b977e844536823ee1a0045244acc42ca6b6107da9b4953736d94898a96cab40845eab77dae21e20e1fa66a3dfc4dedf575dfbca04aa"
)

prepare() {
	cd "$srcdir/libXaw-1.0.16"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/libXaw-1.0.16"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

package() {
	cd "$srcdir/libXaw-1.0.16"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
