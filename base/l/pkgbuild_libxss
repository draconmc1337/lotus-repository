# pkgbuild_libxss
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "libxss"
pkgver  = "1.2.5"
pkgrel  = "1"
pkgdesc = "The XScrnSaver Library"
license = "MIT"
pkgtype = "source"

depends     = ()
makedepends = ("x11"; "xext"; "xextproto"; "scrnsaverproto")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://xorg.freedesktop.org/archive/individual/lib/libXScrnSaver-1.2.5.tar.xz"
md5sums = "5057365f847253e0e275871441e10ff7846c8322a5d88e1e187d326de1cd8d00"

prepare() {
	cd "$srcdir/libXScrnSaver-1.2.5  # extracted dir: libXScrnSaver-1.2.5"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/libXScrnSaver-1.2.5  # extracted dir: libXScrnSaver-1.2.5"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

package() {
	cd "$srcdir/libXScrnSaver-1.2.5  # extracted dir: libXScrnSaver-1.2.5"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
