# PKGBUILD — libxpm
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "libxpm"
pkgver  = "3.5.17"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "MIT"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "glibc"
    "libx11"
    "libxext"
    "libxt"
)

makedepends = (
    "glib2"
    "git"
    "xorg-util-macros"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://www.x.org/releases/individual/lib/libXpm-3.5.17.tar.xz"
)

checksums = (
    "sha512:52f9d2664a47a26c1a6ad65d18867de870b66947b0b0d99cca3512756a0aaa6ce2a245c0b49f20b70c3ce48bf04c47c333e8119a147465c277bca727f6ab017e"
)

prepare() {
	cd "$srcdir/libXpm-3.5.17"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/libXpm-3.5.17"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

check() {
	cd "$srcdir/libXpm-3.5.17"
	make check
}

package() {
	cd "$srcdir/libXpm-3.5.17"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
