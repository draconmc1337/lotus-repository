# pkgbuild_libxfixes
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "libxfixes"
pkgver  = "6.0.2"
pkgrel  = "1"
pkgdesc = "X Fixes Library"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ("xproto"; "fixesproto"; "xextproto"; "x11")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://xorg.freedesktop.org/archive/individual/lib/libXfixes-6.0.2.tar.xz"
md5sums = "39f115d72d9c5f8111e4684164d3d68cc1fd21f9b27ff2401b08fddfc0f409ba"

prepare() {
	cd "$srcdir/libXfixes-6.0.2  # extracted dir: libXfixes-6.0.2"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/libXfixes-6.0.2  # extracted dir: libXfixes-6.0.2"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

package() {
	cd "$srcdir/libXfixes-6.0.2  # extracted dir: libXfixes-6.0.2"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
