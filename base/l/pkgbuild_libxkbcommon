# pkgbuild_libxkbcommon
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "libxkbcommon"
pkgver  = "1.7.0"
pkgrel  = "1"
pkgdesc = "XKB API common to servers and clients"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ("xkeyboard-config"; "xkbcommon"; "xcb"; "xcb-xkb"; "xkbcommon-x11"; "libxml-2.0"; "xkbregistry"; "bash-completion"; "wayland-client"; "wayland-protocols"; "wayland-scanner"; "icu-uc")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/xkbcommon/libxkbcommon/archive/refs/tags/xkbcommon-1.7.0.tar.gz"
md5sums = "20d5e40dabd927f7a7f4342bebb1e8c7a59241283c978b800ae3bf60394eabc4"

prepare() {
	cd "$srcdir/libxkbcommon-xkbcommon-1.7.0  # extracted dir: libxkbcommon-xkbcommon-1.7.0"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/libxkbcommon-xkbcommon-1.7.0  # extracted dir: libxkbcommon-xkbcommon-1.7.0"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/libxkbcommon-xkbcommon-1.7.0  # extracted dir: libxkbcommon-xkbcommon-1.7.0"
	ninja -C build test
}

package() {
	cd "$srcdir/libxkbcommon-xkbcommon-1.7.0  # extracted dir: libxkbcommon-xkbcommon-1.7.0"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
