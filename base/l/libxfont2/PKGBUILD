# pkgbuild_libxfont2
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "libxfont2"
pkgver  = "2.0.7"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "MIT"
pkgtype = "source"

depends     = ()
makedepends = ("libbsd-overlay"; "Z_REQUIRES=zlib"; "zlib")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://xorg.freedesktop.org/archive/individual/lib/libXfont2-2.0.7.tar.xz"
md5sums = "8b7b82fdeba48769b69433e8e3fbb984a5f6bf368b0d5f47abeec49de3e58efb"

prepare() {
	cd "$srcdir/libXfont2-2.0.7  # extracted dir: libXfont2-2.0.7"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/libXfont2-2.0.7  # extracted dir: libXfont2-2.0.7"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

check() {
	cd "$srcdir/libXfont2-2.0.7  # extracted dir: libXfont2-2.0.7"
	make check
}

package() {
	cd "$srcdir/libXfont2-2.0.7  # extracted dir: libXfont2-2.0.7"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
