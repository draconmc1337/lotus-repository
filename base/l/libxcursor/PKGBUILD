# PKGBUILD — libxcursor
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "libxcursor"
pkgver  = "1.2.3"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "default-cursors"
    "glibc"
    "libx11"
    "libxfixes"
    "libxrender"
    "xorgproto"
)

makedepends = (
    "xorg-util-macros"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://www.x.org/releases/individual/lib/libXcursor-1.2.3.tar.xz"
)

checksums = (
    "sha512:069a1eb27a0ee1b29b251bb6c2d0688543a791d6862fad643279e86736e1c12ca6fc02b85b8611c225a9735dc00efab84672d42b547baa97304362f0c5ae0b5a"
)

prepare() {
	cd "$srcdir/libXcursor-1.2.3"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/libXcursor-1.2.3"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

package() {
	cd "$srcdir/libXcursor-1.2.3"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
