# musl compatibility patch for wpa_supplicant
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-common.patch

# Issue: common.h:14 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: common.h:630 — mcheck.h
# Fix:   remove — musl has no mtrace

