# PKGBUILD — wpa_supplicant
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "wpa_supplicant"
pkgver  = "2.11"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "BSD"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "glibc"
    "libdbus"
    "libnl"
    "openssl"
    "pcsclite"
    "readline"
)

makedepends = (
    "docbook-sgml"
    "docbook-utils"
    "git"
    "perl-sgmls"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://w1.fi/releases/wpa_supplicant-2.11.tar.gz"
    "patches/musl-trace.patch"
    "patches/musl-trace.patch"
    "patches/musl-common.patch"
    "patches/musl-driver_bsd.patch"
    "patches/musl-l2_packet_freebsd.patch"
)

checksums = (
    "sha512:9a0a3a9d6fa2235903c40aa57b5955f0c9dd1dccfd0e3825a3b6f92b3e32db8d464b3ea0aef3285ba3ee109e7b190560cedd744902e954f0003cdba543e277b2"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-trace.patch"
	patch -p1 < "$srcdir/patches/musl-trace.patch"
	patch -p1 < "$srcdir/patches/musl-common.patch"
	patch -p1 < "$srcdir/patches/musl-driver_bsd.patch"
	patch -p1 < "$srcdir/patches/musl-l2_packet_freebsd.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	# TODO: add build commands
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	# TODO: install files
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
