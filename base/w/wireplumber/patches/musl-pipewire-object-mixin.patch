# musl compatibility patch for wireplumber
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-pipewire-object-mixin.patch

# Issue: pipewire-object-mixin.c:14 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/pipewire-object-mixin.c
+++ b/pipewire-object-mixin.c
@@ -14,1 +14,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

