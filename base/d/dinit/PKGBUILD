# PKGBUILD — dinit
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "dinit"
pkgver  = "0.19.3"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "Apache-2.0"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    # TODO: add build deps
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/davmac314/dinit/archive/v0.19.3/dinit-0.19.3.tar.gz"
)

checksums = (
    "sha512:541ade391ed6fd43da394e19152ba63267614742a60b8e219e03db1ef91dd1c884e157aaad020c2e08b0e3b580a0dd7deb1b17392fdae5691d4062e5a17d02b6"
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
