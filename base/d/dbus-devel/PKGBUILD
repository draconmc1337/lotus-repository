# pkgbuild_dbus-devel
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "dbus-devel"
pkgver  = "1.15.12"
pkgrel  = "1"
pkgdesc = "The maximum number of connections that can be handled at once"
license = "GPL-2.0"
pkgtype = "source"

depends     = ()
makedepends = (""; "gio-windows-2.0"; "gio-unix-2.0"; "expat"; "libselinux"; "libapparmor"; "libsystemd"; "systemd"; "x11")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://gitlab.freedesktop.org/dbus/dbus/-/archive/dbus-1.15.12/dbus-dbus-1.15.12.tar.gz"
md5sums = "c98dc3add81a30baa2d452f2c9a35fb178e35558ccd2e593c0251681c8864c88"

prepare() {
	cd "$srcdir/dbus-dbus-1.15.12  # extracted dir: dbus-dbus-1.15.12"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/dbus-dbus-1.15.12  # extracted dir: dbus-dbus-1.15.12"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared \
		-Dsystemd=disabled
	ninja -C build
}

check() {
	cd "$srcdir/dbus-dbus-1.15.12  # extracted dir: dbus-dbus-1.15.12"
	ninja -C build test
}

package() {
	cd "$srcdir/dbus-dbus-1.15.12  # extracted dir: dbus-dbus-1.15.12"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
