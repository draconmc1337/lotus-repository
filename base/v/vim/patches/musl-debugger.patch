# musl compatibility patch for vim
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-debugger.patch

# Issue: debugger.c:21 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/debugger.c
+++ b/debugger.c
@@ -21,1 +21,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: debugger.c:252 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/debugger.c
+++ b/debugger.c
@@ -252,1 +252,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: debugger.c:257 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/debugger.c
+++ b/debugger.c
@@ -257,1 +257,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: debugger.c:366 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/debugger.c
+++ b/debugger.c
@@ -366,1 +366,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

