# pkgbuild_vulkan-headers
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "vulkan-headers"
pkgver  = "1.3.302"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ()
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/KhronosGroup/Vulkan-Headers/archive/refs/tags/v1.3.302.tar.gz"
md5sums = "996c3f4220971e3b3cd6b8933e9e81f0bc82b6d2d6449b6f02c66946d65bf944"

prepare() {
	cd "$srcdir/Vulkan-Headers-1.3.302  # extracted dir: Vulkan-Headers-1.3.302"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/Vulkan-Headers-1.3.302  # extracted dir: Vulkan-Headers-1.3.302"
	cmake -B build -S . \
		-DCMAKE_BUILD_TYPE=Release \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DBUILD_SHARED_LIBS=ON
	cmake --build build --parallel $(nproc)
}

check() {
	cd "$srcdir/Vulkan-Headers-1.3.302  # extracted dir: Vulkan-Headers-1.3.302"
	cmake --build build --target test
}

package() {
	cd "$srcdir/Vulkan-Headers-1.3.302  # extracted dir: Vulkan-Headers-1.3.302"
	DESTDIR="$pkgdir" cmake --install build
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
