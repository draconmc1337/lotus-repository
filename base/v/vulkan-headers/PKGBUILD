# PKGBUILD — vulkan-headers
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "vulkan-headers"
pkgver  = "1.4.304.0"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "cmake"
    "git"
    "ninja"
    "python"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/KhronosGroup/Vulkan-Headers/archive/vulkan-sdk-1.4.304.0/Vulkan-Headers-vulkan-sdk-1.4.304.0.tar.gz"
)

checksums = (
    "sha512:25a1afe5383e640c28d56d4b04e80be80d8db248ee38e7c7e1a7cb2d5052a505cf4ea0c6b2c5c12b6616f7f391d2a729e9a3b497ad1b0f39ce049846da5de946"
)

prepare() {
	cd "$srcdir/Vulkan-Headers-vulkan-sdk-1.4.304.0"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/Vulkan-Headers-vulkan-sdk-1.4.304.0"
	cmake -B build -S . \
		-DCMAKE_BUILD_TYPE=Release \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DBUILD_SHARED_LIBS=ON
	cmake --build build --parallel $(nproc)
}

check() {
	cd "$srcdir/Vulkan-Headers-vulkan-sdk-1.4.304.0"
	cmake --build build --target test
}

package() {
	cd "$srcdir/Vulkan-Headers-vulkan-sdk-1.4.304.0"
	DESTDIR="$pkgdir" cmake --install build
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
