# musl compatibility patch for busybox
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-init.c.patch.patch

# Issue: init.c:144 — execinfo.h
# Fix:   use libexecinfo or remove backtrace
--- a/init.c
+++ b/init.c
@@ -144,1 +144,5 @@
-#include <execinfo.h>
+#ifdef __GLIBC__
+#include <execinfo.h>
+#endif

# Issue: init.c:1026 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/init.c
+++ b/init.c
@@ -1026,1 +1026,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: init.c:1027 — backtrace_symbols
# Fix:   link -lexecinfo or remove

