# musl compatibility patch for busybox
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-libbb.patch

# Issue: libbb.h:49 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: libbb.h:1385 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: libbb.h:1859 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

