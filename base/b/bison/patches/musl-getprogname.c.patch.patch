# musl compatibility patch for bison
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-getprogname.c.patch.patch

# Issue: getprogname.c:22 — program_invocation_name
# Fix:   use argv[0] instead
--- a/getprogname.c
+++ b/getprogname.c
@@ -22,1 +22,5 @@
-program_invocation_name
+/* program_invocation_name not in musl */
+extern char *__progname; /* musl provides this */

# Issue: getprogname.c:71 — program_invocation_name
# Fix:   use argv[0] instead
--- a/getprogname.c
+++ b/getprogname.c
@@ -71,1 +71,5 @@
-program_invocation_name
+/* program_invocation_name not in musl */
+extern char *__progname; /* musl provides this */

