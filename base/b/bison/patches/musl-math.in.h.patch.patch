# musl compatibility patch for bison
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-math.in.h.patch.patch

# Issue: math.in.h:283 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: math.in.h:325 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: math.in.h:367 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: math.in.h:432 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: math.in.h:502 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: math.in.h:522 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: math.in.h:554 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: math.in.h:610 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: math.in.h:686 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: math.in.h:726 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

