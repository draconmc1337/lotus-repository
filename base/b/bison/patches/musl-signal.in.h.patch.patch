# musl compatibility patch for bison
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-signal.in.h.patch.patch

# Issue: signal.in.h:65 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: signal.in.h:155 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: signal.in.h:181 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: signal.in.h:346 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

