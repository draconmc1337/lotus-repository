# musl compatibility patch for bison
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-progname.c.patch.patch

# Issue: progname.c:25 — program_invocation_name
# Fix:   use argv[0] instead
--- a/progname.c
+++ b/progname.c
@@ -25,1 +25,5 @@
-program_invocation_name
+/* program_invocation_name not in musl */
+extern char *__progname; /* musl provides this */

# Issue: progname.c:87 — program_invocation_name
# Fix:   use argv[0] instead
--- a/progname.c
+++ b/progname.c
@@ -87,1 +87,5 @@
-program_invocation_name
+/* program_invocation_name not in musl */
+extern char *__progname; /* musl provides this */

# Issue: progname.c:90 — program_invocation_name
# Fix:   use argv[0] instead
--- a/progname.c
+++ b/progname.c
@@ -90,1 +90,5 @@
-program_invocation_name
+/* program_invocation_name not in musl */
+extern char *__progname; /* musl provides this */

