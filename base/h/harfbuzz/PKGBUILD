# PKGBUILD — harfbuzz
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "harfbuzz"
pkgver  = "10.2.0"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "MPL-2.0"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "freetype2"
    "glib2"
    "glibc"
    "graphite"
    "libfreetype"
    "libglib-2"
    "libgobject-2"
    "libgraphite2"
    "libpng"
    "libpng16"
    "libz"
    "zlib"
)

makedepends = (
    "freetype2"
    "FreeType"
    "Freetype"
    "glib-2.0"
    "gobject-2.0"
    "graphite2"
    "icu-uc"
    "ICU"
    "cairo"
    "cairo-ft"
    "chafa"
    "appleframeworks"
    "glib2"
    "git"
    "glew"
    "glfw"
    "glib2-devel"
    "glibc"
    "gobject-introspection"
    "graphite"
    "gtk-doc"
    "help2man"
    "icu"
    "libgl"
    "libpng"
    "libstdc++"
    "meson"
    "python"
    "ragel"
    "zlib"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/harfbuzz/harfbuzz/releases/download/10.2.0/harfbuzz-10.2.0.tar.xz"
)

checksums = (
    "sha512:522028a5de91a042832b1634fc4b7636b1b42c5ee258882d155bc33fca7b30de19ca714b4f9ea8dc3d3f537142ca2305fcf5af04bec4edbf608f557c12742e54"
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	ninja -C build test
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
