# musl compatibility patch for m4
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-strerror_r.c.patch.patch

# Issue: strerror_r.c:37 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: strerror_r.c:46 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: strerror_r.c:55 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

