# musl compatibility patch for m4
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-mmap-anon-util.h.patch.patch

# Issue: mmap-anon-util.h:23 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/mmap-anon-util.h
+++ b/mmap-anon-util.h
@@ -23,1 +23,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

