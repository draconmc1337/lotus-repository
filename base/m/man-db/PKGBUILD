# PKGBUILD — man-db
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "man-db"
pkgver  = "2.13.0"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "GPL-2.0"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "bash"
    "gdbm"
    "glibc"
    "groff"
    "less"
    "libpipeline"
    "libseccomp"
    "zlib"
)

makedepends = (
    "libpipeline"
    "po4a"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://download.savannah.gnu.org/releases/man-db/man-db-2.13.0.tar.xz"
    "patches/musl-zsoelim_main.patch"
    "patches/musl-zsoelim.patch"
    "patches/musl-whatis.patch"
    "patches/musl-ult_src.patch"
    "patches/musl-get-mtime.patch"
    "patches/musl-straycats.patch"
    "patches/musl-manpath.patch"
    "patches/musl-manp.patch"
    "patches/musl-mandb.patch"
    "patches/musl-manconv_main.patch"
    "patches/musl-manconv.patch"
    "patches/musl-man.patch"
    "patches/musl-man-recode.patch"
    "patches/musl-lexgrog_test.patch"
    "patches/musl-lexgrog.patch"
    "patches/musl-globbing_test.patch"
    "patches/musl-globbing.patch"
    "patches/musl-descriptions_store.patch"
)

checksums = (
    "sha512:612ce39bb1e11aa7797e8e3a26f06a7154c48a13b2d74217c2c6701e0d33fa8f77ae2b8c9ee84353e364d16918f884115f2dfbf3cd743edd4ed32b763b87f723"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-zsoelim_main.patch"
	patch -p1 < "$srcdir/patches/musl-zsoelim.patch"
	patch -p1 < "$srcdir/patches/musl-whatis.patch"
	patch -p1 < "$srcdir/patches/musl-ult_src.patch"
	patch -p1 < "$srcdir/patches/musl-get-mtime.patch"
	patch -p1 < "$srcdir/patches/musl-straycats.patch"
	patch -p1 < "$srcdir/patches/musl-manpath.patch"
	patch -p1 < "$srcdir/patches/musl-manp.patch"
	patch -p1 < "$srcdir/patches/musl-mandb.patch"
	patch -p1 < "$srcdir/patches/musl-manconv_main.patch"
	patch -p1 < "$srcdir/patches/musl-manconv.patch"
	patch -p1 < "$srcdir/patches/musl-man.patch"
	patch -p1 < "$srcdir/patches/musl-man-recode.patch"
	patch -p1 < "$srcdir/patches/musl-lexgrog_test.patch"
	patch -p1 < "$srcdir/patches/musl-lexgrog.patch"
	patch -p1 < "$srcdir/patches/musl-globbing_test.patch"
	patch -p1 < "$srcdir/patches/musl-globbing.patch"
	patch -p1 < "$srcdir/patches/musl-descriptions_store.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info \
		--without-systemd
	make -j$(nproc)
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
