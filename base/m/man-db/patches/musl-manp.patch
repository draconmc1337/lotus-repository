# musl compatibility patch for man-db
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-manp.patch

# Issue: manp.c:64 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/manp.c
+++ b/manp.c
@@ -64,1 +64,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: manp.c:1111 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/manp.c
+++ b/manp.c
@@ -1111,1 +1111,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

