# musl compatibility patch for man-db
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-whatis.patch

# Issue: whatis.c:55 — argp.h
# Fix:   bundle argp-standalone or use getopt_long
--- a/whatis.c
+++ b/whatis.c
@@ -55,1 +55,5 @@
-#include <argp.h>
+/* argp not in musl — use getopt_long instead */

# Issue: whatis.c:58 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/whatis.c
+++ b/whatis.c
@@ -58,1 +58,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

