# pkgbuild_mesa
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "mesa"
pkgver  = "24.3.4"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "unknown"
pkgtype = "source"

depends     = ()
makedepends = (""; "libglvnd"; "directx-headers"; "DirectX-Headers"; "vdpau"; "libclc"; "cutils"; "hardware"; "log"; "sync"; "backtrace"; "nativewindow"; "android.hardware.graphics.mapper"; "ui"; "zlib"; "libzstd"; "expat"; "libdrm_"; "libudev"; "LLVMSPIRVLib"; "lua54"; "libelf"; "valgrind"; "libunwind"; "wayland-scanner"; "wayland-protocols"; "wayland-client"; "wayland-server"; "wayland-egl-backend"; "openmp"; "xcb"; "xcb-randr"; "x11"; "xext"; "xfixes"; "xcb-glx"; "xcb-shm"; "xcb-keysyms"; "x11-xcb"; "xcb-dri2"; "xcb-dri3"; "xcb-present"; "xcb-sync"; "xshmfence"; "glproto"; "dri2proto"; "xxf86vm"; "xcb-xfixes"; "xrandr"; "perfetto"; "xtensor"; "flatbuffers")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://gitlab.freedesktop.org/mesa/mesa/-/archive/mesa-24.3.4/mesa-mesa-24.3.4.tar.gz"
md5sums = "966b515c83e6d52f34a151194abd0464b1a7af87158eba4036801a600216288e"

prepare() {
	cd "$srcdir/mesa-mesa-24.3.4  # extracted dir: mesa-mesa-24.3.4"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/mesa-mesa-24.3.4  # extracted dir: mesa-mesa-24.3.4"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

package() {
	cd "$srcdir/mesa-mesa-24.3.4  # extracted dir: mesa-mesa-24.3.4"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
