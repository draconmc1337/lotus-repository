# musl compatibility patch for mesa
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-u_process.patch

# Issue: u_process.c:63 — program_invocation_name
# Fix:   use argv[0] instead
--- a/u_process.c
+++ b/u_process.c
@@ -63,1 +63,5 @@
-program_invocation_name
+/* program_invocation_name not in musl */
+extern char *__progname; /* musl provides this */

# Issue: u_process.c:75 — program_invocation_name
# Fix:   use argv[0] instead
--- a/u_process.c
+++ b/u_process.c
@@ -75,1 +75,5 @@
-program_invocation_name
+/* program_invocation_name not in musl */
+extern char *__progname; /* musl provides this */

# Issue: u_process.c:94 — program_invocation_name
# Fix:   use argv[0] instead
--- a/u_process.c
+++ b/u_process.c
@@ -94,1 +94,5 @@
-program_invocation_name
+/* program_invocation_name not in musl */
+extern char *__progname; /* musl provides this */

# Issue: u_process.c:98 — program_invocation_name
# Fix:   use argv[0] instead
--- a/u_process.c
+++ b/u_process.c
@@ -98,1 +98,5 @@
-program_invocation_name
+/* program_invocation_name not in musl */
+extern char *__progname; /* musl provides this */

