# musl compatibility patch for mesa
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-dri_common_query_renderer.patch

# Issue: dri_common_query_renderer.c:27 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/dri_common_query_renderer.c
+++ b/dri_common_query_renderer.c
@@ -27,1 +27,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

