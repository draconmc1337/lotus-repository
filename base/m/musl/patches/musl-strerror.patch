# musl compatibility patch for musl
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-strerror.patch

# Issue: strerror.c:16 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/strerror.c
+++ b/strerror.c
@@ -16,1 +16,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: strerror.c:20 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/strerror.c
+++ b/strerror.c
@@ -20,1 +20,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: strerror.c:26 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/strerror.c
+++ b/strerror.c
@@ -26,1 +26,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

