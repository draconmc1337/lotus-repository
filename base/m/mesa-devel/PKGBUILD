# PKGBUILD — mesa-devel
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "mesa-devel"
pkgver  = "24.3.3"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "unknown"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "libglvnd"
    "directx-headers"
    "DirectX-Headers"
    "vdpau"
    "libclc"
    "cutils"
    "hardware"
    "log"
    "sync"
    "backtrace"
    "nativewindow"
    "android.hardware.graphics.mapper"
    "ui"
    "zlib"
    "libzstd"
    "expat"
    "libdrm_"
    "libudev"
    "LLVMSPIRVLib"
    "lua54"
    "libelf"
    "valgrind"
    "libunwind"
    "wayland-scanner"
    "wayland-protocols"
    "wayland-client"
    "wayland-server"
    "wayland-egl-backend"
    "openmp"
    "xcb"
    "xcb-randr"
    "x11"
    "xext"
    "xfixes"
    "xcb-glx"
    "xcb-shm"
    "xcb-keysyms"
    "x11-xcb"
    "xcb-dri2"
    "xcb-dri3"
    "xcb-present"
    "xcb-sync"
    "xshmfence"
    "glproto"
    "dri2proto"
    "xxf86vm"
    "xcb-xfixes"
    "xrandr"
    "perfetto"
    "xtensor"
    "flatbuffers"
    "wayland"
    "libxcb"
    "libx11"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://archive.mesa3d.org/mesa-24.3.3.tar.xz"
    "patches/musl-u_thread.patch"
    "patches/musl-u_thread.patch"
    "patches/musl-u_process.patch"
    "patches/musl-blob.patch"
    "patches/musl-bitpack_helpers.patch"
    "patches/musl-panvk_macros.patch"
    "patches/musl-panvk_instance.patch"
    "patches/musl-entry.patch"
    "patches/musl-anv_private.patch"
    "patches/musl-executor_genx.patch"
    "patches/musl-imgui_draw.patch"
    "patches/musl-pvr_private.patch"
    "patches/musl-pvr_bo.patch"
    "patches/musl-gtest-port-arch.patch"
    "patches/musl-query_renderer.patch"
    "patches/musl-glxcurrent.patch"
    "patches/musl-glxcmds.patch"
    "patches/musl-glx_pbuffer.patch"
    "patches/musl-glx_error.patch"
    "patches/musl-driwindows_glx.patch"
    "patches/musl-dri_common_query_renderer.patch"
    "patches/musl-create_context.patch"
    "patches/musl-applegl_glx.patch"
    "patches/musl-invocation.patch"
    "patches/musl-util.patch"
    "patches/musl-invocation.patch"
)

checksums = (
    "sha512:280d7a6dd64503ec398d449430270c78b5558c03bfeed3a42346573fd2db1aa3f96f5a212e49288718a7fe27447ee40235b066517622d68b0dfdd7b251a4e85f"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/mesa-24.3.3"
	patch -p1 < "$srcdir/patches/musl-u_thread.patch"
	patch -p1 < "$srcdir/patches/musl-u_thread.patch"
	patch -p1 < "$srcdir/patches/musl-u_process.patch"
	patch -p1 < "$srcdir/patches/musl-blob.patch"
	patch -p1 < "$srcdir/patches/musl-bitpack_helpers.patch"
	patch -p1 < "$srcdir/patches/musl-panvk_macros.patch"
	patch -p1 < "$srcdir/patches/musl-panvk_instance.patch"
	patch -p1 < "$srcdir/patches/musl-entry.patch"
	patch -p1 < "$srcdir/patches/musl-anv_private.patch"
	patch -p1 < "$srcdir/patches/musl-executor_genx.patch"
	patch -p1 < "$srcdir/patches/musl-imgui_draw.patch"
	patch -p1 < "$srcdir/patches/musl-pvr_private.patch"
	patch -p1 < "$srcdir/patches/musl-pvr_bo.patch"
	patch -p1 < "$srcdir/patches/musl-gtest-port-arch.patch"
	patch -p1 < "$srcdir/patches/musl-query_renderer.patch"
	patch -p1 < "$srcdir/patches/musl-glxcurrent.patch"
	patch -p1 < "$srcdir/patches/musl-glxcmds.patch"
	patch -p1 < "$srcdir/patches/musl-glx_pbuffer.patch"
	patch -p1 < "$srcdir/patches/musl-glx_error.patch"
	patch -p1 < "$srcdir/patches/musl-driwindows_glx.patch"
	patch -p1 < "$srcdir/patches/musl-dri_common_query_renderer.patch"
	patch -p1 < "$srcdir/patches/musl-create_context.patch"
	patch -p1 < "$srcdir/patches/musl-applegl_glx.patch"
	patch -p1 < "$srcdir/patches/musl-invocation.patch"
	patch -p1 < "$srcdir/patches/musl-util.patch"
	patch -p1 < "$srcdir/patches/musl-invocation.patch"
}

build() {
	cd "$srcdir/mesa-24.3.3"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

package() {
	cd "$srcdir/mesa-24.3.3"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
