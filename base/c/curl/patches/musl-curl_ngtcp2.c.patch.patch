# musl compatibility patch for curl
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-curl_ngtcp2.c.patch.patch

# Issue: curl_ngtcp2.c:58 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/curl_ngtcp2.c
+++ b/curl_ngtcp2.c
@@ -58,1 +58,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

