# musl compatibility patch for findutils
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-test-error.c.patch.patch

# Issue: test-error.c:1 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/test-error.c
+++ b/test-error.c
@@ -1,1 +1,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: test-error.c:22 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/test-error.c
+++ b/test-error.c
@@ -22,1 +22,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: test-error.c:47 — error_at_line
# Fix:   rewrite with fprintf+exit

# Issue: test-error.c:48 — error_at_line
# Fix:   rewrite with fprintf+exit

# Issue: test-error.c:49 — error_at_line
# Fix:   rewrite with fprintf+exit

# Issue: test-error.c:55 — error_at_line
# Fix:   rewrite with fprintf+exit

# Issue: test-error.c:57 — error_at_line
# Fix:   rewrite with fprintf+exit

# Issue: test-error.c:59 — error_at_line
# Fix:   rewrite with fprintf+exit

# Issue: test-error.c:61 — error_at_line
# Fix:   rewrite with fprintf+exit

# Issue: test-error.c:72 — error_at_line
# Fix:   rewrite with fprintf+exit

# Issue: test-error.c:74 — error_at_line
# Fix:   rewrite with fprintf+exit

