# musl compatibility patch for findutils
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-test-canonicalize.c.patch.patch

# Issue: test-canonicalize.c:39 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/test-canonicalize.c
+++ b/test-canonicalize.c
@@ -39,1 +39,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

# Issue: test-canonicalize.c:67 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/test-canonicalize.c
+++ b/test-canonicalize.c
@@ -67,1 +67,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

# Issue: test-canonicalize.c:108 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/test-canonicalize.c
+++ b/test-canonicalize.c
@@ -108,1 +108,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

# Issue: test-canonicalize.c:120 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/test-canonicalize.c
+++ b/test-canonicalize.c
@@ -120,1 +120,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

# Issue: test-canonicalize.c:129 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/test-canonicalize.c
+++ b/test-canonicalize.c
@@ -129,1 +129,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

# Issue: test-canonicalize.c:133 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/test-canonicalize.c
+++ b/test-canonicalize.c
@@ -133,1 +133,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

# Issue: test-canonicalize.c:135 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/test-canonicalize.c
+++ b/test-canonicalize.c
@@ -135,1 +135,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

# Issue: test-canonicalize.c:156 — canonicalize_file_name
# Fix:   use realpath(path, NULL) instead
--- a/test-canonicalize.c
+++ b/test-canonicalize.c
@@ -156,1 +156,5 @@
-canonicalize_file_name
+/* replace canonicalize_file_name with realpath */
+#define canonicalize_file_name(p) realpath((p), NULL)

