# musl compatibility patch for findutils
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-test-xstrtol.c.patch.patch

# Issue: test-xstrtol.c:23 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/test-xstrtol.c
+++ b/test-xstrtol.c
@@ -23,1 +23,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: test-xstrtol.c:24 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/test-xstrtol.c
+++ b/test-xstrtol.c
@@ -24,1 +24,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

