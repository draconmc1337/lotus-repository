# PKGBUILD — freetype2
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "freetype2"
pkgver  = "2.13.3"
pkgrel  = "1"
pkgdesc = "A free, high-quality, and portable font engine."
license = "unknown"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "zlib"
    "bzip2"
    "libpng"
    "harfbuzz"
    "libbrotlidec"
    "freetype2"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://downloads.sourceforge.net/freetype/freetype-2.13.3.tar.xz"
    "patches/musl-t42parse.patch"
    "patches/musl-t42objs.patch"
    "patches/musl-t42error.patch"
    "patches/musl-t42drivr.patch"
    "patches/musl-otverror.patch"
    "patches/musl-otvalid.patch"
    "patches/musl-gxverror.patch"
    "patches/musl-gxvalid.patch"
    "patches/musl-bdfdrivr.patch"
    "patches/musl-bdflib.patch"
    "patches/musl-pcfdrivr.patch"
    "patches/musl-pcfread.patch"
    "patches/musl-pcferror.patch"
    "patches/musl-psarrst.patch"
    "patches/musl-pserror.patch"
    "patches/musl-psstack.patch"
    "patches/musl-psft.patch"
    "patches/musl-pserror.patch"
    "patches/musl-psarrst.patch"
    "patches/musl-psintrp.patch"
    "patches/musl-psread.patch"
    "patches/musl-psglue.patch"
    "patches/musl-psfont.patch"
    "patches/musl-ftcbasic.patch"
    "patches/musl-ftcsbits.patch"
    "patches/musl-ftcmru.patch"
    "patches/musl-ftcmanag.patch"
    "patches/musl-ftcimage.patch"
    "patches/musl-ftcglyph.patch"
    "patches/musl-ftccmap.patch"
    "patches/musl-ftccache.patch"
    "patches/musl-ftcerror.patch"
)

checksums = (
    "sha512:600828d7756c8cfa974448ef34ee0db573fb8cfdb2dc1e0358b63c44a03bfd7e3d4384424b9cc5e4749034f60231a550c4b7fcb46694fcacea218787ce305504"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/freetype-2.13.3"
	patch -p1 < "$srcdir/patches/musl-t42parse.patch"
	patch -p1 < "$srcdir/patches/musl-t42objs.patch"
	patch -p1 < "$srcdir/patches/musl-t42error.patch"
	patch -p1 < "$srcdir/patches/musl-t42drivr.patch"
	patch -p1 < "$srcdir/patches/musl-otverror.patch"
	patch -p1 < "$srcdir/patches/musl-otvalid.patch"
	patch -p1 < "$srcdir/patches/musl-gxverror.patch"
	patch -p1 < "$srcdir/patches/musl-gxvalid.patch"
	patch -p1 < "$srcdir/patches/musl-bdfdrivr.patch"
	patch -p1 < "$srcdir/patches/musl-bdflib.patch"
	patch -p1 < "$srcdir/patches/musl-pcfdrivr.patch"
	patch -p1 < "$srcdir/patches/musl-pcfread.patch"
	patch -p1 < "$srcdir/patches/musl-pcferror.patch"
	patch -p1 < "$srcdir/patches/musl-psarrst.patch"
	patch -p1 < "$srcdir/patches/musl-pserror.patch"
	patch -p1 < "$srcdir/patches/musl-psstack.patch"
	patch -p1 < "$srcdir/patches/musl-psft.patch"
	patch -p1 < "$srcdir/patches/musl-pserror.patch"
	patch -p1 < "$srcdir/patches/musl-psarrst.patch"
	patch -p1 < "$srcdir/patches/musl-psintrp.patch"
	patch -p1 < "$srcdir/patches/musl-psread.patch"
	patch -p1 < "$srcdir/patches/musl-psglue.patch"
	patch -p1 < "$srcdir/patches/musl-psfont.patch"
	patch -p1 < "$srcdir/patches/musl-ftcbasic.patch"
	patch -p1 < "$srcdir/patches/musl-ftcsbits.patch"
	patch -p1 < "$srcdir/patches/musl-ftcmru.patch"
	patch -p1 < "$srcdir/patches/musl-ftcmanag.patch"
	patch -p1 < "$srcdir/patches/musl-ftcimage.patch"
	patch -p1 < "$srcdir/patches/musl-ftcglyph.patch"
	patch -p1 < "$srcdir/patches/musl-ftccmap.patch"
	patch -p1 < "$srcdir/patches/musl-ftccache.patch"
	patch -p1 < "$srcdir/patches/musl-ftcerror.patch"
}

build() {
	cd "$srcdir/freetype-2.13.3"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/freetype-2.13.3"
	ninja -C build test
}

package() {
	cd "$srcdir/freetype-2.13.3"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
