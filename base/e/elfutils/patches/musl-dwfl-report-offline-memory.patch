# musl compatibility patch for elfutils
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-dwfl-report-offline-memory.patch

# Issue: dwfl-report-offline-memory.c:22 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/dwfl-report-offline-memory.c
+++ b/dwfl-report-offline-memory.c
@@ -22,1 +22,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

