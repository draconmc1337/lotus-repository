# musl compatibility patch for elfutils
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-readelf.patch

# Issue: readelf.c:23 — argp.h
# Fix:   bundle argp-standalone or use getopt_long
--- a/readelf.c
+++ b/readelf.c
@@ -23,1 +23,5 @@
-#include <argp.h>
+/* argp not in musl — use getopt_long instead */

# Issue: readelf.c:13538 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

