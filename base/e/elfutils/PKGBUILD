# PKGBUILD — elfutils
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "elfutils"
pkgver  = "0.192"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "GPL-2.0"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "glibc"
    "libarchive"
    "libelf"
    "libgcc"
    "libstdc++"
    "sh"
)

makedepends = (
    "libzstd"
    "libcurl"
    "json-c"
    "libmicrohttpd"
    "sqlite3"
    "libarchive"
    "zlib"
    "curl"
    "sqlite"
    "glib2"
    "bzip2"
    "git"
    "libgcc"
    "libstdc++"
    "xz"
    "zstd"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://sourceware.org/elfutils/ftp/0.192/elfutils-0.192.tar.bz2"
    "patches/musl-varlocs.patch"
    "patches/musl-peel_type.patch"
    "patches/musl-next_cfi.patch"
    "patches/musl-low_high_pc.patch"
    "patches/musl-line2addr.patch"
    "patches/musl-funcscopes.patch"
    "patches/musl-funcretval.patch"
    "patches/musl-find-prologues.patch"
    "patches/musl-dwflsyms.patch"
    "patches/musl-dwflmodtest.patch"
    "patches/musl-dwfllines.patch"
    "patches/musl-dwfl-report-offline-memory.patch"
    "patches/musl-dwfl-addr-sect.patch"
    "patches/musl-dwarfcfi.patch"
    "patches/musl-debuginfod_build_id_find.patch"
    "patches/musl-backtrace.patch"
    "patches/musl-allregs.patch"
    "patches/musl-aggregate_size.patch"
    "patches/musl-addrscopes.patch"
    "patches/musl-addrcfi.patch"
    "patches/musl-arlib.patch"
    "patches/musl-unstrip.patch"
    "patches/musl-strip.patch"
    "patches/musl-strings.patch"
    "patches/musl-stacktrace.patch"
    "patches/musl-stack.patch"
    "patches/musl-srcfiles.patch"
    "patches/musl-size.patch"
    "patches/musl-readelf.patch"
    "patches/musl-ranlib.patch"
)

checksums = (
    "sha512:543188f5f2cfe5bc7955a878416c5f252edff9926754e5de0c6c57b132f21d9285c9b29e41281e93baad11d4ae7efbbf93580c114579c182103565fe99bd3909"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-varlocs.patch"
	patch -p1 < "$srcdir/patches/musl-peel_type.patch"
	patch -p1 < "$srcdir/patches/musl-next_cfi.patch"
	patch -p1 < "$srcdir/patches/musl-low_high_pc.patch"
	patch -p1 < "$srcdir/patches/musl-line2addr.patch"
	patch -p1 < "$srcdir/patches/musl-funcscopes.patch"
	patch -p1 < "$srcdir/patches/musl-funcretval.patch"
	patch -p1 < "$srcdir/patches/musl-find-prologues.patch"
	patch -p1 < "$srcdir/patches/musl-dwflsyms.patch"
	patch -p1 < "$srcdir/patches/musl-dwflmodtest.patch"
	patch -p1 < "$srcdir/patches/musl-dwfllines.patch"
	patch -p1 < "$srcdir/patches/musl-dwfl-report-offline-memory.patch"
	patch -p1 < "$srcdir/patches/musl-dwfl-addr-sect.patch"
	patch -p1 < "$srcdir/patches/musl-dwarfcfi.patch"
	patch -p1 < "$srcdir/patches/musl-debuginfod_build_id_find.patch"
	patch -p1 < "$srcdir/patches/musl-backtrace.patch"
	patch -p1 < "$srcdir/patches/musl-allregs.patch"
	patch -p1 < "$srcdir/patches/musl-aggregate_size.patch"
	patch -p1 < "$srcdir/patches/musl-addrscopes.patch"
	patch -p1 < "$srcdir/patches/musl-addrcfi.patch"
	patch -p1 < "$srcdir/patches/musl-arlib.patch"
	patch -p1 < "$srcdir/patches/musl-unstrip.patch"
	patch -p1 < "$srcdir/patches/musl-strip.patch"
	patch -p1 < "$srcdir/patches/musl-strings.patch"
	patch -p1 < "$srcdir/patches/musl-stacktrace.patch"
	patch -p1 < "$srcdir/patches/musl-stack.patch"
	patch -p1 < "$srcdir/patches/musl-srcfiles.patch"
	patch -p1 < "$srcdir/patches/musl-size.patch"
	patch -p1 < "$srcdir/patches/musl-readelf.patch"
	patch -p1 < "$srcdir/patches/musl-ranlib.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info \
		--without-systemd
	make -j$(nproc)
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	make check
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
