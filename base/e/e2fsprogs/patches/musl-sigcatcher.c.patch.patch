# musl compatibility patch for e2fsprogs
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-sigcatcher.c.patch.patch

# Issue: sigcatcher.c:18 — execinfo.h
# Fix:   use libexecinfo or remove backtrace
--- a/sigcatcher.c
+++ b/sigcatcher.c
@@ -18,1 +18,5 @@
-#include <execinfo.h>
+#ifdef __GLIBC__
+#include <execinfo.h>
+#endif

# Issue: sigcatcher.c:379 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/sigcatcher.c
+++ b/sigcatcher.c
@@ -379,1 +379,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: sigcatcher.c:380 — backtrace_symbols
# Fix:   link -lexecinfo or remove

