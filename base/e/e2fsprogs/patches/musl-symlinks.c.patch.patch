# musl compatibility patch for e2fsprogs
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-symlinks.c.patch.patch

# Issue: symlinks.c:316 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: symlinks.c:330 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: symlinks.c:339 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

