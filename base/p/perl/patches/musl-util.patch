# musl compatibility patch for perl
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-util.patch

# Issue: util.c:70 — execinfo.h
# Fix:   use libexecinfo or remove backtrace
--- a/util.c
+++ b/util.c
@@ -70,1 +70,5 @@
-#include <execinfo.h>
+#ifdef __GLIBC__
+#include <execinfo.h>
+#endif

# Issue: util.c:1594 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/util.c
+++ b/util.c
@@ -1594,1 +1594,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: util.c:3870 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: util.c:5050 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/util.c
+++ b/util.c
@@ -5050,1 +5050,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: util.c:5065 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/util.c
+++ b/util.c
@@ -5065,1 +5065,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: util.c:6119 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/util.c
+++ b/util.c
@@ -6119,1 +6119,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: util.c:6313 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/util.c
+++ b/util.c
@@ -6313,1 +6313,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: util.c:6322 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/util.c
+++ b/util.c
@@ -6322,1 +6322,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: util.c:6329 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/util.c
+++ b/util.c
@@ -6329,1 +6329,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: util.c:6331 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/util.c
+++ b/util.c
@@ -6331,1 +6331,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: util.c:6335 — execinfo.h
# Fix:   use libexecinfo or remove backtrace
--- a/util.c
+++ b/util.c
@@ -6335,1 +6335,5 @@
-#include <execinfo.h>
+#ifdef __GLIBC__
+#include <execinfo.h>
+#endif

# Issue: util.c:6335 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/util.c
+++ b/util.c
@@ -6335,1 +6335,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

