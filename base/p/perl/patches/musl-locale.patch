# musl compatibility patch for perl
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-locale.patch

# Issue: locale.c:1082 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/locale.c
+++ b/locale.c
@@ -1082,1 +1082,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: locale.c:1999 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: locale.c:8596 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: locale.c:8737 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

