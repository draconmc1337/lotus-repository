# PKGBUILD — perl
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "perl"
pkgver  = "5.40.0"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "unknown"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "db5"
    "gdbm"
    "glibc"
    "libcrypt"
    "libxcrypt"
)

makedepends = (
    "git"
    "patchutils"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://www.cpan.org/src/5.0/perl-5.40.0.tar.xz"
    "patches/musl-reentr.patch"
    "patches/musl-perl.patch"
    "patches/musl-util.patch"
    "patches/musl-locale.patch"
    "patches/musl-pp.patch"
    "patches/musl-reentr.patch"
    "patches/musl-perlio.patch"
    "patches/musl-proto.patch"
    "patches/musl-util.patch"
)

checksums = (
    "sha512:a2fb1a24c6367b4043f4e929b2d74fc3bad1415e53b791ed1f219f1701064ae21b2bd3164ba95fcf24eaf458bd54433024ccae43725c0bb82a1ec6a98dc7052d"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-reentr.patch"
	patch -p1 < "$srcdir/patches/musl-perl.patch"
	patch -p1 < "$srcdir/patches/musl-util.patch"
	patch -p1 < "$srcdir/patches/musl-locale.patch"
	patch -p1 < "$srcdir/patches/musl-pp.patch"
	patch -p1 < "$srcdir/patches/musl-reentr.patch"
	patch -p1 < "$srcdir/patches/musl-perlio.patch"
	patch -p1 < "$srcdir/patches/musl-proto.patch"
	patch -p1 < "$srcdir/patches/musl-util.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	# TODO: add build commands
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	# TODO: install files
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
