# pkgbuild_pipewire
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "pipewire"
pkgver  = "1.2.7"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "MIT"
pkgtype = "source"

depends     = ()
makedepends = (""; "systemd"; "libsystemd"; "libselinux"; "dbus-1"; "sdl2"; "libdrm"; "readline"; "libavcodec"; "libavformat"; "libavutil"; "opus"; "ncursesw"; "sndfile"; "libmysofa"; "libpulse"; "avahi-client"; "x11-xcb"; "xfixes"; "libcanberra"; "libusb-1.0"; "libcap"; "glib-2.0"; "gio-2.0"; "webrtc-audio-processing-1"; "webrtc-audio-processing"; "epoll-shim"; "libinotify"; "intl"; "alsa"; "openssl"; "lilv-0"; "libffado"; "libapparmor"; "snapd-glib-2"; "snapd-glib")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://gitlab.freedesktop.org/pipewire/pipewire/-/archive/1.2.7/pipewire-1.2.7.tar.gz"
md5sums = "e75568ed18bcbe75e9779af57cb9cc256fd7ebfaadc12bb347a0717055d1d3a9"

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared \
		-Dsystemd=disabled
	ninja -C build
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	ninja -C build test
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
