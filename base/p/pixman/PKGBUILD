# pkgbuild_pixman
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "pixman"
pkgver  = "0.44.2"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = (""; "openmp"; "gtk+-3.0"; "glib-2.0"; "libpng")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://gitlab.freedesktop.org/pixman/pixman/-/archive/pixman-0.44.2/pixman-pixman-0.44.2.tar.gz"
md5sums = "1ac33e229549f6341a677d530d1944431139d222ecad1554818788ae9ef32f6f"

prepare() {
	cd "$srcdir/pixman-pixman-0.44.2  # extracted dir: pixman-pixman-0.44.2"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/pixman-pixman-0.44.2  # extracted dir: pixman-pixman-0.44.2"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/pixman-pixman-0.44.2  # extracted dir: pixman-pixman-0.44.2"
	ninja -C build test
}

package() {
	cd "$srcdir/pixman-pixman-0.44.2  # extracted dir: pixman-pixman-0.44.2"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
