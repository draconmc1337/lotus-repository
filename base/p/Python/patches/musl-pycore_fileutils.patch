# musl compatibility patch for Python
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-pycore_fileutils.patch

# Issue: pycore_fileutils.h:288 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/pycore_fileutils.h
+++ b/pycore_fileutils.h
@@ -288,1 +288,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

