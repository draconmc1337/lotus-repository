# musl compatibility patch for Python
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-alloc-override.patch

# Issue: alloc-override.c:254 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: alloc-override.c:275 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

