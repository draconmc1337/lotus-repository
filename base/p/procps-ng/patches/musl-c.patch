# musl compatibility patch for procps-ng
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-c.patch

# Issue: c.h:20 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/c.h
+++ b/c.h
@@ -20,1 +20,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: c.h:127 — error_at_line
# Fix:   rewrite with fprintf+exit

# Issue: c.h:129 — error_at_line
# Fix:   rewrite with fprintf+exit

