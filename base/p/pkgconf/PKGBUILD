# PKGBUILD — pkgconf
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "pkgconf"
pkgver  = "2.3.0"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "glibc"
)

makedepends = (
    "libpkgconf"
    "git"
    "meson"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/pkgconf/pkgconf/archive/pkgconf-2.3.0/pkgconf-2.3.0.tar.gz"
)

checksums = (
    "sha512:0759e63b9bfdc1e0b2f95e4c5c7be24b4ffa22e06a08cfad7f8e8be9faf99c256b1cbc72159e4ec805791d6b507a80fc344d05d63a1d273c3285dac868bf6ca0"
)

prepare() {
	cd "$srcdir/pkgconf-pkgconf-2.3.0"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/pkgconf-pkgconf-2.3.0"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/pkgconf-pkgconf-2.3.0"
	ninja -C build test
}

package() {
	cd "$srcdir/pkgconf-pkgconf-2.3.0"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
