# musl compatibility patch for pipewire
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-module-zeroconf-discover.patch

# Issue: module-zeroconf-discover.c:24 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/module-zeroconf-discover.c
+++ b/module-zeroconf-discover.c
@@ -24,1 +24,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

