# musl compatibility patch for pipewire
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-module-raop-discover.patch

# Issue: module-raop-discover.c:23 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/module-raop-discover.c
+++ b/module-raop-discover.c
@@ -23,1 +23,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

