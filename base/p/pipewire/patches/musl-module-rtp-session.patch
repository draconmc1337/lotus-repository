# musl compatibility patch for pipewire
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-module-rtp-session.patch

# Issue: module-rtp-session.c:32 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/module-rtp-session.c
+++ b/module-rtp-session.c
@@ -32,1 +32,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

