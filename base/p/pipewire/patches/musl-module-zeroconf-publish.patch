# musl compatibility patch for pipewire
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-module-zeroconf-publish.patch

# Issue: module-zeroconf-publish.c:23 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/module-zeroconf-publish.c
+++ b/module-zeroconf-publish.c
@@ -23,1 +23,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

