# musl compatibility patch for shadow
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-commonio.patch

# Issue: commonio.c:73 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: commonio.c:75 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: commonio.c:78 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: commonio.c:80 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: commonio.c:82 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: commonio.c:92 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: commonio.c:94 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

