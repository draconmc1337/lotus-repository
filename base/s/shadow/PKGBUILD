# PKGBUILD — shadow
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "shadow"
pkgver  = "4.16.0"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "BSD"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "acl"
    "audit"
    "glibc"
    "libacl"
    "libaudit"
    "libcrypt"
    "libpam"
    "libpam_misc"
    "libxcrypt"
    "pam"
)

makedepends = (
    "cmocka"
    "libbsd-overlay"
    "glib2"
    "acl"
    "audit"
    "docbook-xsl"
    "git"
    "itstool"
    "libcap"
    "libxcrypt"
    "libxslt"
    "pam"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/shadow-maint/shadow/releases/download/4.16.0/shadow-4.16.0.tar.xz"
    "patches/musl-commonio.patch"
)

checksums = (
    "sha512:3986572c74cce013c733c592ef5a8113e69bba8fee74fb697fe8206bd616d29553e42822e663478db81b3e17b26b403bc69b3492dd981b87643b0e1c55035ecf"
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-commonio.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info \
		--without-systemd
	make -j$(nproc)
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	make check
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
