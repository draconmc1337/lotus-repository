# musl compatibility patch for xorg-server-devel
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-mieq.patch

# Issue: mieq.c:229 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/mieq.c
+++ b/mieq.c
@@ -229,1 +229,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: mieq.c:244 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/mieq.c
+++ b/mieq.c
@@ -244,1 +244,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

