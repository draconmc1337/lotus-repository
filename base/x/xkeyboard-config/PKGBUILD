# PKGBUILD — xkeyboard-config
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "xkeyboard-config"
pkgver  = "2.43"
pkgrel  = "1"
pkgdesc = "X Keyboard configuration data"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "libxslt"
    "meson"
    "python"
    "xorg-xkbcomp"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://gitlab.freedesktop.org/xkeyboard-config/xkeyboard-config/-/archive/xkeyboard-config-2.43/xkeyboard-config-xkeyboard-config-2.43.tar.gz"
)

checksums = (
    "sha512:2f46c303d8d4b2848275b670616173cb5fee99c3ea65a031b5a9709f1ea543183a6c022adf8a30c6993ce859307046b2b45f775e4bae6c6967e256c89b45a073"
)

prepare() {
	cd "$srcdir/xkeyboard-config-xkeyboard-config-2.43"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/xkeyboard-config-xkeyboard-config-2.43"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/xkeyboard-config-xkeyboard-config-2.43"
	ninja -C build test
}

package() {
	cd "$srcdir/xkeyboard-config-xkeyboard-config-2.43"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
