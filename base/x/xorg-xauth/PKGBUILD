# pkgbuild_xorg-xauth
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "xorg-xauth"
pkgver  = "1.1.5"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ()
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://xorg.freedesktop.org/archive/individual/app/xauth-1.1.5.tar.xz"
md5sums = "a4000e2f441facebf569026bedecc23ba262cc6927be52070abe0002625cfbe0"

prepare() {
	cd "$srcdir/xauth-1.1.5  # extracted dir: xauth-1.1.5"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/xauth-1.1.5  # extracted dir: xauth-1.1.5"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

check() {
	cd "$srcdir/xauth-1.1.5  # extracted dir: xauth-1.1.5"
	make check
}

package() {
	cd "$srcdir/xauth-1.1.5  # extracted dir: xauth-1.1.5"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
