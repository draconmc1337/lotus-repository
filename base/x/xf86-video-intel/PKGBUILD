# PKGBUILD — xf86-video-intel
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "xf86-video-intel"
pkgver  = "2.99.917"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "MIT"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "glibc"
    "libdrm"
    "libpciaccess"
    "libx11"
    "libxcb"
    "libxdamage"
    "libxext"
    "libxfixes"
    "libxrender"
    "libxshmfence"
    "libxvmc"
    "mesa"
    "pixman"
    "systemd-libs"
    "xcb-util"
)

makedepends = (
    "gen4asm=yes"
    "udev="yes""
    "x11="yes""
    "x11_dri3="yes""
    "ivo_requires="$ivo_requires"
    "ivo="yes""
    "have_dri1=$DRI"
    "have_dri2=$DRI"
    "have_dri3=$DRI"
    "PRESENT="yes""
    "libpng"
    "cairo"
    "libxcb"
    "libx11"
    "X-ABI-VIDEODRV_VERSION"
    "git"
    "libxcursor"
    "libxinerama"
    "libxrandr"
    "libxss"
    "libxtst"
    "libxv"
    "xorg-server-devel"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://www.x.org/releases/individual/driver/xf86-video-intel-2.99.917.tar.bz2"
    "patches/musl-sna_display.patch"
    "patches/musl-xassert.patch"
    "patches/musl-sna_driver.patch"
    "patches/musl-kgem.patch"
    "patches/musl-sna_accel.patch"
    "patches/musl-intel_device.patch"
)

checksums = (
    "sha512:cbf4d46ad1ad5e5587c0f1f620ff534ef0645270517b60056b9f03e83d8216e2f456de46352a06c37c0c46963cc4ed20b71b815b20ec1bf680ff046e535f580f"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-sna_display.patch"
	patch -p1 < "$srcdir/patches/musl-xassert.patch"
	patch -p1 < "$srcdir/patches/musl-sna_driver.patch"
	patch -p1 < "$srcdir/patches/musl-kgem.patch"
	patch -p1 < "$srcdir/patches/musl-sna_accel.patch"
	patch -p1 < "$srcdir/patches/musl-intel_device.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	make check
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
