# pkgbuild_xorg-server
# Lotus Linux package recipe — repo: base
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "xorg-server"
pkgver  = "21.1.21"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ("xproto"; "randrproto"; "renderproto"; "xextproto"; "inputproto"; "kbproto"; "fontsproto"; "fixesproto"; "damageproto"; "xcmiscproto"; "bigreqsproto"; "xtrans"; "videoproto"; "compositeproto"; "recordproto"; "scrnsaverproto"; "resourceproto"; "xf86driproto"; "dri2proto"; "dri3proto"; "xineramaproto"; "xf86bigfontproto"; "xf86vidmodeproto"; "applewmproto"; "xshmfence"; "pixman-1"; "libbsd-overlay"; "xkbcomp"; "xkbfile"; "xfont2"; "dbus-1"; "libsystemd"; "libsystemd-daemon"; "fontutil"; "libxcvt"; "xext"; "x11"; "xau"; "hal"; ""; "libudev"; "gbm"; "epoxy"; "libsha1"; "nettle"; "libgcrypt"; "openssl"; "xdmcp"; "libdrm"; "xf86dgaproto"; "libselinux"; "audit"; "libunwind"; "xorg-sgml-doctools")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://xorg.freedesktop.org/archive/individual/xserver/xorg-server-21.1.21.tar.xz"
md5sums = "c0cbe5545b3f645bae6024b830d1d1154a956350683a4e52b2fff5b0fa1ab519"

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared \
		-Dsystemd=disabled
	ninja -C build
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	ninja -C build test
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
