# PKGBUILD — xf86-video-amdgpu
# Lotus Linux  |  repo: base
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "xf86-video-amdgpu"
pkgver  = "23.0.0"
pkgrel  = "1"
pkgdesc = "# TODO: add description"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    "glibc"
    "libdrm"
    "mesa"
    "systemd-libs"
)

makedepends = (
    "LIBUDEV=yes"
    "glib2"
    "X-ABI-VIDEODRV_VERSION"
    "meson"
    "systemd"
    "xorg-server-devel"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://www.x.org/releases/individual/driver/xf86-video-amdgpu-23.0.0.tar.xz"
)

checksums = (
    "sha512:bf26f147629a34e84a0ae8435119e170b9c95edafcab1995b63bb8f55abef32f2efbf4536eb070e64b2ae1460424b1b27a4206cb9836d33ddc6dfbee404f718b"
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
