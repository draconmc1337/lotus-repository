# pkgbuild_waybar
# Lotus Linux package recipe — repo: extra
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "waybar"
pkgver  = "0.11.0"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "MIT"
pkgtype = "source"

depends     = ()
makedepends = ("fmt"; "spdlog"; "wayland-client"; "wayland-cursor"; "wayland-protocols"; "gtkmm-3.0"; "dbusmenu-gtk3-0.4"; "gio-unix-2.0"; "jsoncpp"; "sigc++-2.0"; "libinotify"; "epoll-shim"; "libinput"; "libnl-3.0"; "libnl-genl-3.0"; "upower-glib"; "libpipewire-0.3"; "playerctl"; "libpulse"; "libudev"; "libevdev"; "libmpdclient"; "xkbregistry"; "jack"; "wireplumber-0.5"; ""; "gtk-layer-shell-0"; "systemd"; "date"; "cava"; "scdoc"; "gtk+3")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/Alexays/Waybar/archive/refs/tags/0.11.0.tar.gz"
md5sums = "6a0e9f0f7f2eff503951958cbb16dc39041c0b67e86c35154e8507677c61be9d"

prepare() {
	cd "$srcdir/Waybar-0.11.0  # extracted dir: Waybar-0.11.0"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/Waybar-0.11.0  # extracted dir: Waybar-0.11.0"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared \
		-Dsystemd=disabled
	ninja -C build
}

check() {
	cd "$srcdir/Waybar-0.11.0  # extracted dir: Waybar-0.11.0"
	ninja -C build test
}

package() {
	cd "$srcdir/Waybar-0.11.0  # extracted dir: Waybar-0.11.0"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
