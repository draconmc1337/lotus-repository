# pkgbuild_playerctl
# Lotus Linux package recipe — repo: extra
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "playerctl"
pkgver  = "2.4.1"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "LGPL-2.1"
pkgtype = "source"

depends     = ()
makedepends = ("gobject-2.0"; "gio-unix-2.0"; "glib-2.0"; "bash-completion")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/altdesktop/playerctl/archive/refs/tags/v2.4.1.tar.gz"
md5sums = "75957ad5071956f563542c7557af16a57e40b4a7f66bc9b6373d022ec5eef548"

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	ninja -C build test
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
