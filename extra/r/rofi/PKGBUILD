# PKGBUILD — rofi
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "rofi"
pkgver  = "1.7.7"
pkgrel  = "1"
pkgdesc = "Window switcher and dmenu replacement"
license = "MIT"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "glib-2.0"
    "gmodule-2.0"
    "cairo"
    "gio-unix-2.0"
    "pango"
    "pangocairo"
    "xkbcommon"
    "gdk-pixbuf-2.0"
    "xcb"
    "xcb-aux"
    "xcb-xkb"
    "xkbcommon-x11"
    "xcb-ewmh"
    "xcb-icccm"
    "xcb-randr"
    "xcb-cursor"
    "xcb-xinerama"
    "xcb-keysyms"
    "cairo-xcb"
    "libstartup-notification-1.0"
    "xcb-imdkit"
    "check"
    "glib2"
    "libxcb"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/davatorium/rofi/releases/download/1.7.7/rofi-1.7.7.tar.xz"
    "patches/musl-theme-parser.patch"
)

checksums = (
    "sha512:8286bc5f33ab4eb93677b24815730f159347a1e545ff109784fe161eeb4500c4fd4307c7894adb2060d6bef5137c9d6506a71c565e2676be230e5ad740bed9d0"
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-theme-parser.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	ninja -C build test
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
