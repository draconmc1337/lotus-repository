# musl compatibility patch for rxvt-unicode
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-ev.c.patch.patch

# Issue: ev.c:287 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: ev.c:336 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: ev.c:368 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: ev.c:384 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: ev.c:392 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: ev.c:400 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

