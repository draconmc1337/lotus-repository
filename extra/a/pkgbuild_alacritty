# pkgbuild_alacritty
# Lotus Linux package recipe — repo: extra
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "alacritty"
pkgver  = "0.15.1"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "unknown"
pkgtype = "source"

depends     = ()
makedepends = ()
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/alacritty/alacritty/archive/refs/tags/v0.15.1.tar.gz"
md5sums = "b814e30c6271ae23158c66e0e2377c3600bb24041fa382a36e81be564eeb2e36"

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	export CARGO_NET_OFFLINE=true
	cargo build --release --locked
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	install -Dm755 target/release/$pkgname \
		"$pkgdir/usr/bin/$pkgname"
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
