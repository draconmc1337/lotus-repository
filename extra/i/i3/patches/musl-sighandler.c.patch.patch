# musl compatibility patch for i3
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-sighandler.c.patch.patch

# Issue: sighandler.c:26 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/sighandler.c
+++ b/sighandler.c
@@ -26,1 +26,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: sighandler.c:49 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/sighandler.c
+++ b/sighandler.c
@@ -49,1 +49,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

# Issue: sighandler.c:284 — backtrace(
# Fix:   link -lexecinfo or remove
--- a/sighandler.c
+++ b/sighandler.c
@@ -284,1 +284,5 @@
-backtrace(
+#ifdef __GLIBC__
+    backtrace(...);
+#endif

