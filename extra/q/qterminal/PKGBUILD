# PKGBUILD — qterminal
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "qterminal"
pkgver  = "2.1.0"
pkgrel  = "1"
pkgdesc = "LXQt terminal emulator"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "qt6core"
    "qt6gui"
    "qt6linguisttools"
    "qt6widgets"
    "layershellqt"
    "qt6dbus"
    "qt6"
    "qtermwidget6"
    "lxqt2-build-tools"
    "x11"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/lxqt/qterminal/releases/download/2.1.0/qterminal-2.1.0.tar.xz"
)

checksums = (
    "sha512:3787bb8269a4b06c8c42d0c1c5e900a85e868e36e415f4ae05bbcb1fde3cbdf88a2ada7a86dd39a4a07305586fcb19e2ab04182bdd9f7f10dbf6cd4fd1194f22"
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	cmake -B build -S . \
		-DCMAKE_BUILD_TYPE=Release \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DBUILD_SHARED_LIBS=ON
	cmake --build build --parallel $(nproc)
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	cmake --build build --target test
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" cmake --install build
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
