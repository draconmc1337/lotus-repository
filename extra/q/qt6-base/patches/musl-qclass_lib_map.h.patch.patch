# musl compatibility patch for qt6-base
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-qclass_lib_map.h.patch.patch

# Issue: qclass_lib_map.h:309 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/qclass_lib_map.h
+++ b/qclass_lib_map.h
@@ -309,1 +309,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: qclass_lib_map.h:372 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/qclass_lib_map.h
+++ b/qclass_lib_map.h
@@ -372,1 +372,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: qclass_lib_map.h:393 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/qclass_lib_map.h
+++ b/qclass_lib_map.h
@@ -393,1 +393,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

