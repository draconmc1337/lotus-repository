# musl compatibility patch for qt6-base
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-qsqltablemodel.cpp.patch.patch

# Issue: qsqltablemodel.cpp:7 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/qsqltablemodel.cpp
+++ b/qsqltablemodel.cpp
@@ -7,1 +7,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

