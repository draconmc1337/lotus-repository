# pkgbuild_gtk-layer-shell
# Lotus Linux package recipe — repo: extra
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "gtk-layer-shell"
pkgver  = "0.9.0"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "unknown"
pkgtype = "source"

depends     = ()
makedepends = ("gtk+-3.0"; "wayland-client"; "wayland-server"; "wayland-scanner"; "wayland-protocols")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/wmww/gtk-layer-shell/archive/refs/tags/v0.9.0.tar.gz"
md5sums = "3809e5565d9ed02e44bb73787ff218523e8760fef65830afe60ea7322e22da1c"

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	ninja -C build test
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
