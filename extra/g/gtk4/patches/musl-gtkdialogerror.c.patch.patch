# musl compatibility patch for gtk4
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-gtkdialogerror.c.patch.patch

# Issue: gtkdialogerror.c:21 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/gtkdialogerror.c
+++ b/gtkdialogerror.c
@@ -21,1 +21,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

