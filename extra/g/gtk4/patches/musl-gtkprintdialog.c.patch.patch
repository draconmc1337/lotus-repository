# musl compatibility patch for gtk4
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-gtkprintdialog.c.patch.patch

# Issue: gtkprintdialog.c:25 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/gtkprintdialog.c
+++ b/gtkprintdialog.c
@@ -25,1 +25,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

