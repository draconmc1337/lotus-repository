# musl compatibility patch for gtk4
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-gtkcsstokenizer.c.patch.patch

# Issue: gtkcsstokenizer.c:23 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/gtkcsstokenizer.c
+++ b/gtkcsstokenizer.c
@@ -23,1 +23,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

