# musl compatibility patch for gtk4
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-gtkcolordialog.c.patch.patch

# Issue: gtkcolordialog.c:27 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/gtkcolordialog.c
+++ b/gtkcolordialog.c
@@ -27,1 +27,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

