# PKGBUILD — gobject-introspection
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "gobject-introspection"
pkgver  = "1.82.0"
pkgrel  = "1"
pkgdesc = "GObject introspection"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "glib-2.0"
    "gobject-2.0"
    "gio-2.0"
    "gmodule-2.0"
    "gio-unix-2.0"
    "gio-windows-2.0"
    "libffi"
    "cairo"
    "cairo-gobject"
    "gobject-introspection-1.0"
    "glib2"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://download.gnome.org/sources/gobject-introspection/1.82/gobject-introspection-1.82.0.tar.xz"
    "patches/musl-glib-2.0.patch"
    "patches/musl-gio-2.0.patch"
)

checksums = (
    "sha512:e139fadb4174c72b648914f3774d89fc0e5eaee45bba0c13edf05de883664dad8276dbc34006217bb09871ed4bad23adab51ff232a17b9eb131329b2926cafb7"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	patch -p1 < "$srcdir/patches/musl-glib-2.0.patch"
	patch -p1 < "$srcdir/patches/musl-gio-2.0.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	ninja -C build test
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
