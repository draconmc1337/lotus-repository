# PKGBUILD — xfdesktop
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "xfdesktop"
pkgver  = "4.20.0"
pkgrel  = "1"
pkgdesc = "XFCE4 desktop manager"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "glib-2.0"
    "gio-unix-2.0"
    "gtk+-3.0"
    "libxfce4util-1.0"
    "libxfce4ui-2"
    "libxfce4kbd-private-3"
    "libxfce4windowing-0"
    "libxfce4windowingui-0"
    "libxfconf-0"
    "exo-2"
    "garcon-1"
    "garcon-gtk3-1"
    "thunarx-3"
    "libnotify"
    "x11"
    "gdk-x11-3.0"
    "libxfce4windowing-x11-0"
    "gdk-wayland-3.0"
    "gtk-layer-shell-0"
    "yaml-0.1"
    "gtk+3"
    "cairo"
    "glib2"
    "wayland"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://archive.xfce.org/src/xfce/xfdesktop/4.20/xfdesktop-4.20.0.tar.bz2"
)

checksums = (
    "sha512:84b0c87c9678d3d6170af3d6edabe983282b0bd4d696e32d3a1eaaca99651d56387a9767645d5b48afdc517a18503e42fdaf0a72af9a73144475fb3b6183219c"
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

check() {
	cd "$srcdir/$pkgname-$pkgver"
	ninja -C build test
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
