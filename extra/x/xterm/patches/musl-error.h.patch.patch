# musl compatibility patch for xterm
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-error.h.patch.patch

# Issue: error.h:1 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/error.h
+++ b/error.h
@@ -1,1 +1,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: error.h:54 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/error.h
+++ b/error.h
@@ -54,1 +54,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

