# musl compatibility patch for xterm
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-main.c.patch.patch

# Issue: main.c:136 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/main.c
+++ b/main.c
@@ -136,1 +136,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: main.c:199 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: main.c:312 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: main.c:3665 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: main.c:4501 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: main.c:5021 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: main.c:5603 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

