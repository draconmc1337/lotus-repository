# PKGBUILD — xserver
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "xserver"
pkgver  = "21.1.13"
pkgrel  = "1"
pkgdesc = "X.Org X server"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "xproto"
    "randrproto"
    "renderproto"
    "xextproto"
    "inputproto"
    "kbproto"
    "fontsproto"
    "fixesproto"
    "damageproto"
    "xcmiscproto"
    "bigreqsproto"
    "xtrans"
    "videoproto"
    "compositeproto"
    "recordproto"
    "scrnsaverproto"
    "resourceproto"
    "xf86driproto"
    "dri2proto"
    "dri3proto"
    "xineramaproto"
    "xf86bigfontproto"
    "xf86vidmodeproto"
    "applewmproto"
    "xshmfence"
    "pixman-1"
    "libbsd-overlay"
    "xkbcomp"
    "xkbfile"
    "xfont2"
    "dbus-1"
    "libsystemd"
    "libsystemd-daemon"
    "fontutil"
    "libxcvt"
    "xext"
    "x11"
    "xau"
    "hal"
    "libudev"
    "gbm"
    "epoxy"
    "libsha1"
    "nettle"
    "libgcrypt"
    "openssl"
    "xdmcp"
    "libdrm"
    "xf86dgaproto"
    "libselinux"
    "audit"
    "libunwind"
    "xorg-sgml-doctools"
    "glib2"
    "wayland"
    "libxcb"
    "libx11"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://xorg.freedesktop.org/releases/individual/xserver/xorg-server-21.1.13.tar.xz"
    "patches/musl-backtrace.patch"
    "patches/musl-osinit.patch"
    "patches/musl-glamor.patch"
    "patches/musl-os.patch"
    "patches/musl-misc.patch"
    "patches/musl-kdrive.patch"
    "patches/musl-xf86pciBus.patch"
    "patches/musl-xf86Events.patch"
    "patches/musl-bsd_init.patch"
    "patches/musl-xf86_OSlib.patch"
    "patches/musl-xprFrame.patch"
    "patches/musl-stub.patch"
    "patches/musl-bundle-main.patch"
    "patches/musl-inpututils.patch"
    "patches/musl-mieq.patch"
)

checksums = (
    "sha512:a55fbeeed227c12c67f166f2c06a7f4f8d78feeea04c6e73509dbc723185fd0772349aa23f7c44cf0828ac0a0e2f9e4b26cffb220e6dfa7186d60f88b25ccaf1"
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
    "SKIP"  # musl patch — review before using sha512
)

prepare() {
	cd "$srcdir/xorg-server-21.1.13"
	patch -p1 < "$srcdir/patches/musl-backtrace.patch"
	patch -p1 < "$srcdir/patches/musl-osinit.patch"
	patch -p1 < "$srcdir/patches/musl-glamor.patch"
	patch -p1 < "$srcdir/patches/musl-os.patch"
	patch -p1 < "$srcdir/patches/musl-misc.patch"
	patch -p1 < "$srcdir/patches/musl-kdrive.patch"
	patch -p1 < "$srcdir/patches/musl-xf86pciBus.patch"
	patch -p1 < "$srcdir/patches/musl-xf86Events.patch"
	patch -p1 < "$srcdir/patches/musl-bsd_init.patch"
	patch -p1 < "$srcdir/patches/musl-xf86_OSlib.patch"
	patch -p1 < "$srcdir/patches/musl-xprFrame.patch"
	patch -p1 < "$srcdir/patches/musl-stub.patch"
	patch -p1 < "$srcdir/patches/musl-bundle-main.patch"
	patch -p1 < "$srcdir/patches/musl-inpututils.patch"
	patch -p1 < "$srcdir/patches/musl-mieq.patch"
}

build() {
	cd "$srcdir/xorg-server-21.1.13"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared \
		-Dsystemd=disabled
	ninja -C build
}

check() {
	cd "$srcdir/xorg-server-21.1.13"
	ninja -C build test
}

package() {
	cd "$srcdir/xorg-server-21.1.13"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
