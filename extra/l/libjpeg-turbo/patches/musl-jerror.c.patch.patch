# musl compatibility patch for libjpeg-turbo
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-jerror.c.patch.patch

# Issue: jerror.c:28 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/jerror.c
+++ b/jerror.c
@@ -28,1 +28,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: jerror.c:41 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/jerror.c
+++ b/jerror.c
@@ -41,1 +41,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: jerror.c:42 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/jerror.c
+++ b/jerror.c
@@ -42,1 +42,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: jerror.c:48 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/jerror.c
+++ b/jerror.c
@@ -48,1 +48,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

