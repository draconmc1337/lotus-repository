# musl compatibility patch for libjpeg-turbo
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-jpeglib.h.patch.patch

# Issue: jpeglib.h:18 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/jpeglib.h
+++ b/jpeglib.h
@@ -18,1 +18,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: jpeglib.h:1204 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/jpeglib.h
+++ b/jpeglib.h
@@ -1204,1 +1204,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: jpeglib.h:1209 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/jpeglib.h
+++ b/jpeglib.h
@@ -1209,1 +1209,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

