# PKGBUILD — libxrender
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "libxrender"
pkgver  = "0.9.11"
pkgrel  = "1"
pkgdesc = "X Rendering Extension client library"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    # TODO: add build deps
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://xorg.freedesktop.org/releases/individual/lib/libXrender-0.9.11.tar.xz"
)

checksums = (
    "sha512:2327cc1b530382d81df29b8d579f801f1f990202107cc84506986bcf5d15010d414e9418987b200e3f222d188afce46f0d7754febcdca62a5fc1e541b3bb82de"
)

prepare() {
	cd "$srcdir/libXrender-0.9.11"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/libXrender-0.9.11"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

package() {
	cd "$srcdir/libXrender-0.9.11"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
