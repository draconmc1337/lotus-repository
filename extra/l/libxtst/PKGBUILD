# PKGBUILD — libxtst
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "libxtst"
pkgver  = "1.2.4"
pkgrel  = "1"
pkgdesc = "X Test extension library"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "xext"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://xorg.freedesktop.org/releases/individual/lib/libXtst-1.2.4.tar.xz"
)

checksums = (
    "sha512:f2f4e01239b892b8e26284cede90feb4fe7738beb243bca0f6adb349029f8bf87b5a482911edcd2401733c92ef78eec07fcd455b92e5b8c4828374769e60098d"
)

prepare() {
	cd "$srcdir/libXtst-1.2.4"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/libXtst-1.2.4"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make -j$(nproc)
}

package() {
	cd "$srcdir/libXtst-1.2.4"
	make DESTDIR="$pkgdir" install
	find "$pkgdir" -name '*.la' -delete
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
