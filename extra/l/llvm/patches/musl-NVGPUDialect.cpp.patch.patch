# musl compatibility patch for llvm
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-NVGPUDialect.cpp.patch.patch

# Issue: NVGPUDialect.cpp:410 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/NVGPUDialect.cpp
+++ b/NVGPUDialect.cpp
@@ -410,1 +410,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: NVGPUDialect.cpp:433 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/NVGPUDialect.cpp
+++ b/NVGPUDialect.cpp
@@ -433,1 +433,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: NVGPUDialect.cpp:457 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/NVGPUDialect.cpp
+++ b/NVGPUDialect.cpp
@@ -457,1 +457,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

# Issue: NVGPUDialect.cpp:470 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/NVGPUDialect.cpp
+++ b/NVGPUDialect.cpp
@@ -470,1 +470,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

