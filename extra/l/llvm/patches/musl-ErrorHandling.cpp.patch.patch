# musl compatibility patch for llvm
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-ErrorHandling.cpp.patch.patch

# Issue: ErrorHandling.cpp:243 — error.h
# Fix:   replace error() with fprintf(stderr,...)
--- a/ErrorHandling.cpp
+++ b/ErrorHandling.cpp
@@ -243,1 +243,5 @@
-#include <error.h>
+/* error.h not available on musl */
+#define error(status,...) do { fprintf(stderr,__VA_ARGS__); if(status) exit(status); } while(0)

