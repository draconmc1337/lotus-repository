# musl compatibility patch for llvm
# Generated by LotusPacMan v4.0.0
# Apply with: patch -p1 < musl-generate_siginfo.c.patch.patch

# Issue: generate_siginfo.c:40 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

# Issue: generate_siginfo.c:64 — __GLIBC__
# Fix:   guard with #ifdef __GLIBC__ ... #endif

