# PKGBUILD — lxqt-runner
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "lxqt-runner"
pkgver  = "2.1.0"
pkgrel  = "1"
pkgdesc = "LXQt application runner"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "qt6widgets"
    "qt6xml"
    "qt6linguisttools"
    "kf6windowsystem"
    "lxqt"
    "lxqt-globalkeys-ui"
    "layershellqt"
    "pkgconfig"
    "menucache"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/lxqt/lxqt-runner/releases/download/2.1.0/lxqt-runner-2.1.0.tar.xz"
)

checksums = (
    "sha512:27806c85c0f20217f2beba4a8f8d8512ec6995b7b5573ebf04f1cf7f0bfda74987d879629b00432f20f27cef9d7fa2817909ca47e57a2e40d8dbe55bd22d2231"
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	cmake -B build -S . \
		-DCMAKE_BUILD_TYPE=Release \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DBUILD_SHARED_LIBS=ON
	cmake --build build --parallel $(nproc)
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" cmake --install build
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
