# PKGBUILD — lxqt-panel
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "lxqt-panel"
pkgver  = "2.1.0"
pkgrel  = "1"
pkgdesc = "LXQt panel"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "qt6dbus"
    "qt6linguisttools"
    "qt6widgets"
    "qt6xml"
    "kf6windowsystem"
    "lxqt"
    "lxqt-globalkeys-ui"
    "lxqt-menu-data"
    "layershellqt"
    "pulseaudio"
    "alsa"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/lxqt/lxqt-panel/releases/download/2.1.0/lxqt-panel-2.1.0.tar.xz"
)

checksums = (
    "sha512:8729f06bdac891f505e6a7e584f892d556e22507291dc54ef7e4809c883b245c76112cd2d1518a1172f053e515c11a94cb79f25b9ebd40f213504e73e8b6a5a6"
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	cmake -B build -S . \
		-DCMAKE_BUILD_TYPE=Release \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DBUILD_SHARED_LIBS=ON
	cmake --build build --parallel $(nproc)
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" cmake --install build
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
