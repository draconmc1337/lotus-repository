# pkgbuild_libpulse
# Lotus Linux package recipe — repo: extra
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "libpulse"
pkgver  = "17.0"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ("bash-completion"; ""; "dbus-1"; "glib-2.0"; "sndfile"; "libsystemd"; "x11-xcb"; "fftw3f"; "libasyncns"; "gtk+-3.0"; "tdb"; "alsa"; "gio-2.0"; "orc-0.4"; "samplerate"; "speexdsp"; "soxr"; "webrtc-audio-processing-1"; "systemd"; "libelogind"; "xcb"; "ice"; "sm"; "xtst"; "avahi-client"; "sbc"; "bluez"; "jack"; "lirc"; "openssl"; "libudev"; "gstreamer-1.0"; "gstreamer-app-1.0"; "gstreamer-rtp-1.0"; "check")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://gitlab.freedesktop.org/pulseaudio/pulseaudio/-/archive/v17.0/pulseaudio-v17.0.tar.gz"
md5sums = "896eab02c40783fc0a5dfd098b6f3ec1586686651ee4dcac573dcef666f4531f"

prepare() {
	cd "$srcdir/pulseaudio-v17.0  # extracted dir: pulseaudio-v17.0"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/pulseaudio-v17.0  # extracted dir: pulseaudio-v17.0"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared \
		-Dsystemd=disabled
	ninja -C build
}

package() {
	cd "$srcdir/pulseaudio-v17.0  # extracted dir: pulseaudio-v17.0"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
