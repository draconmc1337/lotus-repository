# PKGBUILD — liblxqt
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "liblxqt"
pkgver  = "2.1.0"
pkgrel  = "1"
pkgdesc = "LXQt core library"
license = "custom"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    "lxqt2-build-tools"
    "qt6"
    "qt6xdg"
    "kf6windowsystem"
    "polkitqt6-1"
    "x11"
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/lxqt/liblxqt/releases/download/2.1.0/liblxqt-2.1.0.tar.xz"
)

checksums = (
    "sha512:98f234ddab09e01f9ac105826ec43171bdffd220d8b462776b5c157a691cbc24a2d8e601ed46f8e55d435403f5518a42a103b151ee3e367eaa909bfc535e45a9"
)

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	cmake -B build -S . \
		-DCMAKE_BUILD_TYPE=Release \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DBUILD_SHARED_LIBS=ON
	cmake --build build --parallel $(nproc)
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	DESTDIR="$pkgdir" cmake --install build
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
