# pkgbuild_ttf-liberation
# Lotus Linux package recipe — repo: extra
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "ttf-liberation"
pkgver  = "2.1.5"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ()
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/liberationfonts/liberation-fonts/archive/refs/tags/2.1.5.tar.gz"
md5sums = "9a823ccb33c8a8a865e84b982bcdc44d03ba4914adb91e6000df035dc0e55936"

prepare() {
	cd "$srcdir/liberation-fonts-2.1.5  # extracted dir: liberation-fonts-2.1.5"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/liberation-fonts-2.1.5  # extracted dir: liberation-fonts-2.1.5"
	make -j$(nproc) PREFIX=/usr
}

package() {
	cd "$srcdir/liberation-fonts-2.1.5  # extracted dir: liberation-fonts-2.1.5"
	make DESTDIR="$pkgdir" install
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
