# pkgbuild_foot
# Lotus Linux package recipe — repo: extra
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "foot"
pkgver  = "1.21.0"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "MIT"
pkgtype = "source"

depends     = ()
makedepends = ("epoll-shim"; "pixman-1"; "wayland-protocols"; "wayland-client"; "wayland-cursor"; "xkbcommon"; "fontconfig"; "libutf8proc"; "tllist"; "fcft"; "wayland-scanner"; "systemd"; "scdoc")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://codeberg.org/dnkl/foot/archive/1.21.0.tar.gz"
md5sums = "b93b196a3fbab86678c54be627557bdc7b1fc8042d99b14c4a74b149f60bcd52"

prepare() {
	cd "$srcdir/foot  # extracted dir: foot"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/foot  # extracted dir: foot"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared \
		-Dsystemd=disabled
	ninja -C build
}

check() {
	cd "$srcdir/foot  # extracted dir: foot"
	ninja -C build test
}

package() {
	cd "$srcdir/foot  # extracted dir: foot"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
