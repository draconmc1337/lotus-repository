# PKGBUILD — featherpad
# Lotus Linux  |  repo: extra
# Generated by LotusPacMan v4.0.0 — review before committing!

pkgname = "featherpad"
pkgver  = "1.5.0"
pkgrel  = "1"
pkgdesc = "LXQt text editor"
license = "GPL-2.0"
url     = "https://TODO"
pkgtype = "source"

depends = (
    # TODO: add runtime deps
)

makedepends = (
    # TODO: add build deps
)

replaces  = ()
conflicts = ()
backup    = ()

sources = (
    "https://github.com/tsujan/FeatherPad/releases/download/V1.5.0/featherpad-1.5.0.tar.xz"
)

checksums = (
    "sha512:5104fbcb2f83e3bdb9c1394cd546b1d6036d3e00e4e358da05affed895a426b1d63bd4d62edc5c65beed96fa3d4adcdadafb4f5b2eb84406ba651576e49c2bb6"
)

prepare() {
	cd "$srcdir/FeatherPad-1.5.0"
	# patch -p1 < "$srcdir/patches/fix.patch"
}

build() {
	cd "$srcdir/FeatherPad-1.5.0"
	cmake -B build -S . \
		-DCMAKE_BUILD_TYPE=Release \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DBUILD_SHARED_LIBS=ON
	cmake --build build --parallel $(nproc)
}

package() {
	cd "$srcdir/FeatherPad-1.5.0"
	DESTDIR="$pkgdir" cmake --install build
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
