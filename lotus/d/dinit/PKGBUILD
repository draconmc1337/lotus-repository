# pkgbuild_dinit
# Lotus Linux package recipe — repo: lotus
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "dinit"
pkgver  = "0.20.0"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "Apache-2.0"
pkgtype = "source"

depends     = ()
makedepends = ()
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/davmac314/dinit/releases/download/v0.20.0/dinit-0.20.0.tar.xz"
md5sums = "f8e3f92629d9aeb7d09e1859dd1cfc2754c11873e16b407a41025f6e81e9e33c"

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	make -j$(nproc) PREFIX=/usr
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	make install PREFIX="$pkgdir/usr"
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
