# pkgbuild_tuigreet
# Lotus Linux package recipe — repo: lotus
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "tuigreet"
pkgver  = "0.9.1"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "GPL-2.0"
pkgtype = "source"

depends     = ("chrono"; "crossterm"; "futures"; "getopts"; "greetd_ipc"; "i18n-embed"; "i18n-embed-fl"; "lazy_static"; "nix"; "tui"; "rust-embed"; "rust-ini"; "smart-default"; "textwrap"; "tokio"; "unic-langid"; "zeroize"; "uzers"; "rand"; "tracing-appender"; "tracing-subscriber"; "tracing")
makedepends = ()
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/apognu/tuigreet/archive/refs/tags/0.9.1.tar.gz"
md5sums = "14fd1fadeb84040eb31901da2b53a48aa55b0fdaccb36d96fa52ce2d2113667f"

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	export CARGO_NET_OFFLINE=true
	cargo build --release --locked
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	install -Dm755 target/release/$pkgname \
		"$pkgdir/usr/bin/$pkgname"
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
