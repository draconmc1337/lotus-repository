# pkgbuild_ttf-iosevka-nerd
# Lotus Linux package recipe — repo: lotus
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "ttf-iosevka-nerd"
pkgver  = "0.0.0"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "custom"
pkgtype = "source"

depends     = ()
makedepends = ()
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/Iosevka.tar.xz"
md5sums = "213ee24cda99ca84d0a8326de133e7e8b2baf9ba23659ce829f589f771d357d2"

prepare() {
	cd "$srcdir/lpm-src-KGL2nm  # extracted dir: lpm-src-KGL2nm"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/lpm-src-KGL2nm  # extracted dir: lpm-src-KGL2nm"
	# TODO: add build commands
}

package() {
	cd "$srcdir/lpm-src-KGL2nm  # extracted dir: lpm-src-KGL2nm"
	# TODO: install files manually
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
