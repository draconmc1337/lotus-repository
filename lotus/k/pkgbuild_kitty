# pkgbuild_kitty
# Lotus Linux package recipe — repo: lotus
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "kitty"
pkgver  = "0.39.0"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "GPL-2.0"
pkgtype = "source"

depends     = ()
makedepends = ()
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/kovidgoyal/kitty/archive/refs/tags/v0.39.0.tar.gz"
md5sums = "fa0b8a649225907f86d14ed6c5e9013b788030f9c6fab44c168928619b0b7f29"

prepare() {
	cd "$srcdir/$pkgname-$pkgver"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/$pkgname-$pkgver"
	export CGO_ENABLED=0
	go build -trimpath \
		-ldflags='-s -w' \
		-o $pkgname ./...
}

package() {
	cd "$srcdir/$pkgname-$pkgver"
	install -Dm755 $pkgname "$pkgdir/usr/bin/$pkgname"
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
