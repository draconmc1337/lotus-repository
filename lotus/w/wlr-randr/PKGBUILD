# pkgbuild_wlr-randr
# Lotus Linux package recipe — repo: lotus
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "wlr-randr"
pkgver  = "0.4.1"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "MIT"
pkgtype = "source"

depends     = ()
makedepends = ("wayland-client")
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://git.sr.ht/~emersion/wlr-randr/archive/v0.4.1.tar.gz"
md5sums = "8af085aeed3132f7442a05bafeadfd0ea30e9a4818af1a91e161f7d560f2cd93"

prepare() {
	cd "$srcdir/wlr-randr-v0.4.1  # extracted dir: wlr-randr-v0.4.1"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/wlr-randr-v0.4.1  # extracted dir: wlr-randr-v0.4.1"
	meson setup build \
		--prefix=/usr \
		--buildtype=release \
		-Ddefault_library=shared
	ninja -C build
}

package() {
	cd "$srcdir/wlr-randr-v0.4.1  # extracted dir: wlr-randr-v0.4.1"
	DESTDIR="$pkgdir" ninja -C build install
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
