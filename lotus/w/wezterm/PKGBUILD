# pkgbuild_wezterm
# Lotus Linux package recipe — repo: lotus
# Generated by LotusPacMan v3.0.0
# Review and test before committing!

pkgname = "wezterm"
pkgver  = "5046fc22"
pkgrel  = "1"
pkgdesc = "TODO: add description"
license = "MIT"
pkgtype = "source"

depends     = ()
makedepends = ()
replaces    = ()
conflicts   = ()
backup      = ()

source  = "https://github.com/wez/wezterm/archive/refs/tags/20240203-110809-5046fc22.tar.gz"
md5sums = "3a02a68016ef48cdb2127308be20acafe94a568411e95b0e64dc590cf877b401"

prepare() {
	cd "$srcdir/wezterm-20240203-110809-5046fc22  # extracted dir: wezterm-20240203-110809-5046fc22"
	# patch -p1 < "$srcdir/fix.patch"
}

build() {
	cd "$srcdir/wezterm-20240203-110809-5046fc22  # extracted dir: wezterm-20240203-110809-5046fc22"
	export CARGO_NET_OFFLINE=true
	cargo build --release --locked
}

package() {
	cd "$srcdir/wezterm-20240203-110809-5046fc22  # extracted dir: wezterm-20240203-110809-5046fc22"
	install -Dm755 target/release/$pkgname \
		"$pkgdir/usr/bin/$pkgname"
	# strip binaries + shared libs
	find "$pkgdir" -type f \( -name '*.so*' -o -perm /0111 \) 2>/dev/null \
		| xargs -r strip --strip-unneeded 2>/dev/null || true
	# remove unwanted docs
	rm -rf "$pkgdir/usr/share/doc" "$pkgdir/usr/share/info"
}
